# СТАТУС app-v2 — ТОЧКА ВХОДА И ЖИВАЯ ПАМЯТЬ ПРОЕКТА

> **ЭТОТ ФАЙЛ ЧИТАЕТСЯ ПЕРВЫМ, КАЖДОЙ СЕССИЕЙ.** Он же — журнал: сюда пишут назад.
> Обновлён: **25.07.2026**. ⚡ ВАЖНО: с 25.07 v2 задеплоен И НА БОЕВОЙ `roman-nutrition-app` (указание Замира).

## 🔴 ПРОТОКОЛ (соблюдать строго, каждый чат)

1. **Сначала читай.** До любой работы — прочитай этот файл целиком, потом
   `../CLAUDE.md` (правила проекта, «не трогать формулы»).
2. **Потом пиши назад.** По ходу и в конце сессии дописывай сюда: что прочитал,
   что изменил, что залил, что осталось. Записи — в «Журнал сессий» внизу
   (дата + кратко). Цель: любой новый чат продолжает без раскопок, все работают
   из одной картины.
3. **Сверься, где работаешь.** Боевой `roman-nutrition-app` или тестовый
   `roman-app-v2` — чтобы не залить не туда. Бэкенд у них ОБЩИЙ (см. ниже).
4. Ориентир — **самые последние изменения** (даты файлов + Журнал сессий), а не
   старые снимки. `ОПТИМУМ.md` удалён 11.07 как устаревший (22–23.06) — не воссоздавать.

## Что это
Тестовая копия приложения «рядом» с боевым. Живёт на **https://roman-app-v2.website.yandexcloud.net**
(бакет `roman-app-v2`), боевое приложение НЕ тронуто. Деплой: `bash deploy-v2.sh` (запускает Замир).

## Дизайн
Направление — таб-структура В1 «Тёмное золото» (Питание / Тренировки / Покупки /
Прогресс / Профиль). Актуальный применённый скин — **С2 «Полночь и шампанское»**
(с 09.07, см. `ТЕМЫ.md`). Всё на дизайн-токенах: смена скина = замена блока
переменных в 4 местах (`ТЕМЫ.md`). Пересборка вёрстки в таб-структуру — ещё не сделана
(задача 3), движок подбора при ней НЕ трогать.

## Что уже сделано в v2
- `PLAN_DAYS = 90` (вместо 28), все места на конфиге. Подтверждено в cabinet.html:1107 и index.html:1104.
- Дизайн-слой поверх старого CSS (блок `v2-design` в обоих html), тема С2.
- `workouts.js` — «Тренировки»: 12 пресетов (дом/зал × новичок/продолжающий × цель),
  адаптационный месяц (нед. 1–4), прогрессия +2.5/+5 кг с 5-й недели от рабочего веса
  (localStorage `v2_workout_weights`), кнопка «Сделано» с 7-дневным локом,
  попап с 2 фото (A/B), кнопка «Рабочий вес?».
- `v2-ui.js` — сворачиваемые блоки (данные, активность, сетка 90 дней, тренировки).
- Фикс активности: баллы = max(работа, шаги) + спорт (раньше суммировались → завышение).
- Цены 1499 ₽ (⚠ Продамус-пресет всё ещё 299 — ждём новую ссылку → `PAYMENT_URL`).
- Фото: 56 упражнений ×2 позиции (`workout_photos/<id>.webp` + `<id>_b.webp`).
- gzip для recipes.json и др. в деплое (10.6 МБ → ~1.5 МБ).
- CORS гейтвея: v2-домен добавлен. Вход работает, кука общая с боевым.
- `V2_DEV_NO_AUTH` в cabinet.html — сейчас false (вход настоящий).

## 🍽 СУПЕР-ПРЕСЕТЫ (главное по данным, состояние на 11.07)
- Рабочий файл — `../anchors_seeds.json` (== `../anchors_seeds_FINAL.json`, сборка 10.07 20:37).
  Варианты: `_SUPER` (10.07 11:57), `_C120` (10.07 18:30) — промежуточные, не рабочие.
- **Формат: 238 якорей × 30 УНИКАЛЬНЫХ дней.** Это ЗАДУМАНО: 30 дней при PLAN_DAYS=90
  повторяются ровно **3 раза** (движок: `days[(dayIndex-1) % 30]`, а 90 % 30 = 0 → чистый цикл).
- **Структурная валидация (11.07): чисто.** Все 238 якорей = 30 дней; длина каждого дня
  совпадает с числом слотов; 0 несуществующих id (база 3736 рецептов). Цикл ×3 без сбоев.
- **Качество (`super_presets_report.md`, вердикт «можно заливать»):** микро-покрытие RDA
  18.47→18.54, разнообразие 115→117, рафинад 0.029, макро/UL соблюдены по построению.
- ✅ **ЯКОРЬ #84 (m70 2883) — ЗАКРЫТ, ±3% ДОСТИГНУТ 11.07 (вечер).** Прежний вывод
  «структурно недостижим, лучший день 13.8%» ОКАЗАЛСЯ АРТЕФАКТОМ метода: он получен ЧИСТО
  СЛУЧАЙНЫМ перебором (`fix_anchor84.py`), который не находит тонкую достижимую область.
  При новой цели (жир поднят до 21% = Ж67/У440) + ЛОКАЛЬНЫЙ ПОИСК (координатный спуск по
  макро, `regen_fat21.py`) якорь #84 даёт 200+ строгих дней в ±3%: итоговые 30 дней —
  макро-откл min 0.6 / медиана 1.8 / макс 2.98%, микро 16.96/20, UL соблюдён, 57 уник блюд.
  ВЫВОД на будущее: «недостижимость» через random-сэмплинг НЕ доказывает инфизибилити —
  проверять локальным/направленным поиском. `fix_anchor84.py` устарел (его дни заменены).
- ✅ `validate_presets.py` — ИСПРАВЛЕН 11.07 (вечер): ожидание дней 28→30. Теперь по всему
  файлу: 238 якорей, все по 30 дней, 0 битых id, **0 дней вне ±3%**, 0 превышений UL,
  микро 18.68/20, рафинад 0.029, вердикт ✅ «можно заливать».
- Заливка пресетов в v2 (замена `anchors_seeds.json` + поднять версию/имя из-за force-cache)
  — по СТАТУС ещё НЕ подтверждена выполненной. Уточнить у Замира.

## Открытые задачи (по порядку!)
1. **Перенести правки оригинала** — Замир правил боевые index/cabinet в другом чате.
   Diff: `_baseline_20260708/` (MD5 внутри) vs текущие `../index.html`, `../cabinet.html`.
   Перенести ТОЛЬКО его правки, не задев v2-надстройки. Ждём от Замира «правки готовы».
   ⚠ Внимание: корневой `../cabinet.html` (09.07) и `app-v2/cabinet.html` (10.07) разошлись —
   определить источник истины до переноса, чтобы не задвоить.
2. **Супер-пресеты** — см. блок выше. Долить якорь #84, затем заменить `../anchors_seeds.json` → деплой.
3. **Пересборка в таб-структуру (В1)** — только после 1 и 2. Движок подбора НЕ трогать:
   логика JS остаётся, меняется разметка/CSS. Тест после: `buildDayMenu`+`_daySum` на 3 днях,
   отклонения ≤3%, без DOM-рендера (тяжёлый рендер вешает страницу).
4. **Перед боевым запуском:** Продамус 1499, `ACCESS_DAYS = 95` в облачной функции
   (`backend-changes/README.md`), `V2_DEV_NO_AUTH=false`, свежие фото блюд.

## Опасные зоны (не трогать без нужды)
- Формулы КБЖУ и движок подбора (rebalanceDayMacros, maxWeightWithCaps, seed-режим).
- Потолки порций «ниже минимума» — это НЕ баг (см. `../CLAUDE.md`).
- **Бэкенд ОБЩИЙ с боевым:** правки Cloud Function / гейтвея (`ACCESS_DAYS`, Продамус,
  CORS) бьют по продакшену. В deploy-v2.sh их НЕТ — Замир применяет вручную, осознанно.

---

## Активный курс (с 11.07): работаем ТОЛЬКО над v2
Первую версию забыли — потом v2 встанет на место v1. Все правки/выводы вносим в
`app-v2/cabinet.html` + `app-v2/index.html`. Данные (`../anchors_seeds.json`, пресеты) общие.

**Пол жира набора 21%** — внесён в v2 (кабинет+индекс), корневые возвращены в исходное.
Отпечаток (пол 21%): цель меняется у 2.8% профилей, якорь у 0.6%, не-наборные 0 — чисто.
✅ По жиру ЗАКРЫТО (11.07 вечер): 14 якорей <21% перегенерены под пол 21% — все в ±3%.

**Бэклог корректировок v2 (11.07):**
1. ✅ СДЕЛАНО (11.07 вечер-5): **витрина = как кабинет, С бар-меню (таб-оболочка).** Замир
   уточнил направление: витрина должна ВЫГЛЯДЕТЬ как кабинет (таб-бар Питание/Тренировки/
   Сегодня/Покупки/Профиль + экран «Сегодня»), а не плоская «лента». Раньше `v2-app.js`
   строил оболочку только под `IS_CABINET` и в index не грузился; «в лоб» подключить нельзя —
   гейт `applyGate` держал бы открытым только «Профиль» и залочил бы всю витрину.
   • **Решение — «витрин-режим» `V2_STORE`** (`v2-app.js`): верхний гейт изменён с
     `!IS_CABINET → return` на `typeof IS_CABINET==='undefined' → return`; `const V2_STORE =
     (IS_CABINET===false)`. Теперь та же оболочка работает и в кабинете, и на витрине.
   • **Локов нет:** `applyGate` при `V2_STORE` сразу `return` (не лочит табы, не форсит Профиль).
     Витрина стартует на экране «Сегодня» (`show('today')`), как кабинет.
   • **Пробное/превью, а не локи (по В-«не лочи весь экран»):** `storefrontSetup()` добавляет —
     тизер «Список покупок после оплаты» + 🔒-метку на таб «Покупки» (клик НЕ заблокирован, ведёт
     к тизеру→оплате), пробный баннер в «Питании» (1 демо-день, полное меню — после оплаты),
     подсказку в «Профиле» (бесплатный расчёт КБЖУ + пробное меню). **Тренировки** НЕ трогал —
     `workouts.js` сам рендерит 1 день-превью + CTA «Открыть всё» (гейт `purchased` внутри `render()`),
     на витрине это уже работает.
   • **Лента 90 дней** на витрине скрыта (нет оплаченных дней) — `renderFoodDays`/строй-стрип под
     `!V2_STORE`; показывается только демо-день. Движок/бесплатный расчёт/CTA-оплаты не тронуты.
   • **index.html:** карточке пол+параметры добавлен `id="fixedParamsCard"` (иначе не переедет в
     таб «Профиль», как в кабинете); подключён `<script src="v2-app.js">` (в самый низ, после
     v2-ui.js) — как в cabinet.html.
   • **Кабинет НЕ затронут:** все витрин-ветки под `if (V2_STORE)`; при `IS_CABINET=true`
     V2_STORE=false → поведение 1-в-1 прежнее. Бэкап `snapshot_20260711_215223_витрина-таб`.
     `node --check v2-app.js` — OK. ⚠ Живьём не проверено (file:// заблокирован расширением,
     деплой за Замиром) — **нужна визуальная проверка витрины после деплоя** (В13).
   • ⚠ Развилки Замиру: (a) стартовый экран витрины — «Сегодня» (сейчас) vs «Профиль» (сразу форма);
     (b) набор/маркировка платных табов — сейчас «Покупки» с 🔒, «Тренировки» как превью без метки;
     (c) после расчёта витрина остаётся на «Профиле» (скролл к КБЖУ) — авто-переключать ли на «Сегодня».
2. ✅ СДЕЛАНО (11.07): вегетарианцу просачивалась рыба/мясо. КОРЕНЬ — `recipeHasExcluded`
   смотрел ТОЛЬКО категорию ингредиента по КБЖУ-справочнику; рецепты, помеченные флешем в
   `tags.cats`/`tags.groups` (или с ингредиентом не из справочника, напр. «сало»), проходили.
   Фикс: добавлена `recipeCategorySet()` — категории собираются по ингредиентам + `tags.cats`
   + `tags.groups` (+карта `_GROUP_LABEL_TO_CAT`: мясное→мясо и т.п.). В cabinet+index.
   Протечек по тегам: было 40 → стало 0 (питон-проверка по recipes.json). Ловит и настоящий
   не-веган («Деруны с салом», «Картофель с салом»), что справочник пропускал. fr_234784
   (минтай) блокируется и по ингредиенту, и по тегу. ⚠ Побочно несколько МИС-тегнутых веган-
   блюд (чизкейк с тегом «рыба», омлеты pdf_540xxx с тегом «мясо») теперь тоже режутся —
   безопаснее так; теги стоит почистить в данных отдельной задачей.
3. ✅ СДЕЛАНО: дефолт приёмов = больший из двух (cabinet, `Math.max(_mc.count,_mc.alt)`).
4. ✅ СДЕЛАНО (11.07): «Сделано» (workouts.js). Toggle: отметка хранит НОМЕР недели
   (`v2_workout_done[dayId]=week`); повторное нажатие в ту же неделю снимает зелёное; с новой
   неделей отметка авто-сбрасывается (day «done» только если сохранённая неделя == текущей);
   кнопка больше не `disabled`. Плюс уведомление об АВТО-изменениях: снимок недели
   (`v2_workout_snapshot`) сравнивается при заходе — если прогрессия сама подняла подходы/
   повторы/вес, показываем тост «Программа обновилась к неделе N: …» (`wkToast`/`notifyAutoProgress`).
   Конец адаптации (нед.4→5) не дублируем — его объясняет отдельное поздравление.
5. ✅ СДЕЛАНО (11.07 вечер): движок прогрессии по научно-скорр. спеке (`workouts.js`).
   Компактная double progression: повторы R0→Rmax по +1/нед → пик → сброс+вес раз в цикл;
   подходы +1 раз в 2 цикла (cap 4); разгрузка каждые 6 нед. Неделя = монотонный счётчик
   `v2_wk_week` по ВЫПОЛНЕННЫМ тренировкам (все дни недели «Сделано» → +1 неделя), без капа,
   без %90 → продление бесшовно (planDays = только видимость). baseWeight-якорь `{w,c}`:
   рычаг «повторный ввод веса» пересчитывает все недели от новой базы БЕЗ сброса недели/цикла/
   подходов/повторов (`v2_workout_weights` мигрирует старые числа). Дом — переход на сложнее
   вариант (`v2_workout_variants`, кнопка в попапе). Инкременты весов по классу + половинный
   шаг >5% + микро-шаг жен верх/изоляция. Логика проверена harness'ом (таблица §2.4 сходится).
6. ✅ СДЕЛАНО (11.07 вечер): эргономика секции — статус-баннер цикла (неделя/цикл/фаза:
   адаптация/рост повторов/пик/разгрузка + прогресс-бар повторов + «до +веса N нед /
   разгрузка через M нед»), плашка разгрузки (синяя), плашка адаптации (зелёная) — на токенах С2.
   Тосты об авто-изменениях адаптированы под новый движок; поздравления сохранены.
7. Фото под каждое упражнение — ПОСЛЕДНИМ. ⚠ ДОБАВЛЕНЫ 2 НОВЫХ упражнения (11.07, возрастная
   адаптация) — под них нужны фото: `h_march` («Ходьба на месте с подъёмом колен») и
   `h_step_touch` («Приставные шаги (степ-тач)»). Оба — низкоударное домашнее кардио для
   старших (суставно-щадящий подбор). Формат как у прочих: `workout_photos/<id>.webp` (+`_b`).
9. ✅ СДЕЛАНО (11.07 вечер-4): **возрастная адаптация тренировок + прямой вопрос про СТАЖ**.
   • **Оси персонализации теперь: цель × пол × ВОЗРАСТ × СТАЖ × месяц** (раньше возраст в
     тренировках не использовался вообще, а стаж ВЫВОДИЛСЯ из частоты — подмена понятий).
   • **ВОЗРАСТ** (`workouts.js`): тиры `young<50 / middle 50–64 / older 65+` (пороги по ACSM
     «Older Adults» + NSCA Fragala 2019). Старшим: меньший шаг веса (`ageStepFactor` older×0.5,
     middle×0.75 — микрозагрузка ~0.5 кг/нед по исследованиям), разгрузка ЧАЩЕ (`deloadEveryFor`
     older 4 / middle 5 / young 6 нед), суставно-щадящий `pickExercise` (убирает ударные
     `impact:'high'`, у 65+ не берёт варианты сложнее нормы), подсказка про разминку/темп
     (`ageHint`, синяя плашка). `age` прокинут в `getProfile` из `state.age` (кабинет уже фиксирует).
     `deloadEvery` прошит через `automat/currentWeight/progressionFor/programStatus/suggestHomeVariants`;
     age — через `weightStep`. Harness: young squat 60→80 кг/год, older 60→70.5 кг/год (мягче, чаще deload).
   • **СТАЖ** — прямой вопрос в онбординге (cabinet.html, карточка `trainingExpCard`:
     «Не тренировался / До года / Больше года» → none/lt1/gt1). `getProfile` маппит none/lt1→новичок,
     gt1→продолжающий; для СТАРЫХ юзеров без поля — фолбэк на вывод из частоты (совместимость).
     Частота (gym_*) осталась для числа дней/объёма.
   • **ХРАНЕНИЕ (ИМПРУВЕР):** бэкенд профиля белым списком фильтрует поля
     (`FIXED_FIELDS`+`EDITABLE_FIELDS=activity/goal/exclusions`) → новый top-level ключ он бы ОТБРОСИЛ.
     Поэтому `trainingExp` кладётся ВНУТРЬ разрешённого JSON `exclusions` (как `mealCount`) —
     переживает F5/смену устройства БЕЗ правки бэкенда. `buildProfileFromState`/`applyProfileToState`
     обновлены. `selectTrainingExp` сохраняет в облако (fire-and-forget) и перерисовывает секцию.
   • **НОВЫЕ упражнения** (нужны фото — см. #7): `h_march`, `h_step_touch` (низкоударное кардио).
   • Бэкап: `backups/snapshot_20260711_203427_возраст-стаж/`. `node --check` — OK (workouts.js +
     извлечённый main-script cabinet.html). DOM-рендер не гонял. НЕ деплоил.
   • ⚠ Развилки Замиру: (1) стаж — 2 тира (новичок/продолжающий), «до года» слит с «не тренировался»;
     ввести ли отдельный СРЕДНИЙ тир пресетов? (2) для существующих 65+ юзеров на деплое разгрузка
     сместится 6→4 нед (одноразовая коррекция; в пре-лонче некритично). (3) удлинять ли адаптацию
     (сейчас 4 нед всем) для старших? Оставил 4, чтобы не сдвигать индексацию автомата.
8. ✅ СДЕЛАНО (11.07 вечер-3): **облачная синхронизация прогресса ТРЕНИРОВОК**. Раньше в облако
   (`/progress-save`) шло только питание (`crossed/eaten/locked`); прогресс тренировок жил только
   в localStorage и терялся при смене устройства/чистке кэша. Теперь синхронизируется ВСЁ:
   • **Что синхронизируется дополнительно:** 8 ключей раздела «Тренировки» —
     `v2_workout_done`, `v2_workout_weights`, `v2_workout_variants`, `v2_wk_week`,
     `v2_workout_snapshot`, `v2_workout_variant_suggested`, `v2_wk_congrats`, `v2_workout_prefs`
     (кладутся в поле `workout` того же JSON-блоба; БЭКЕНД НЕ менялся).
   • `cabinet.html`: `_collectProgress()` собирает `workout`; `restoreProgress()` возвращает ключи
     в localStorage (только префиксы `v2_workout_`/`v2_wk_`) и перерисовывает секцию (`window._wkRender`);
     `window._saveProgress` = гейтед-обёртка над `saveProgressDebounced` (дебаунс 600мс).
   • `workouts.js`: `LS.set` после каждой записи дёргает `window._saveProgress`; добавлен хук
     `window._wkRender`.
   • **Гейт `window._wkCloudReady`** (важное): до завершения облачного ПУЛА локальные (пустые при
     старте) ключи тренировок НЕ пишутся в облако — иначе авто-рендер на DOMContentLoaded затёр бы
     облачный прогресс. Флаг ставится в true в `enterCabinet` ПОСЛЕ `restoreProgress`. Восстановление
     пишет напрямую через `localStorage.setItem` (не `LS.set`) → без эхо-сейва.
   • **Мульти-девайс — last-write-wins** (одно облачное состояние на пользователя, как и у питания):
     ок для старта; при параллельной работе на 2 устройствах побеждает последнее сохранение —
     ⚠ отмечено Замиру как поведение, не баг.
   • **Сброс профиля:** отдельного пути сброса в кабинете НЕТ (профиль фиксируется навсегда,
     цель неизменна) → чистить workout-ключи в облаке негде и незачем; если сброс появится —
     нужно очищать и `workout` (отмечено Замиру).
   Бэкап: `backups/snapshot_20260711_192048_cloudsync-тренировки/`. `node --check` — OK (оба файла).
   DOM-рендер не гонял. НЕ деплоил.

## ✅ 13.07: ПРОДЛЕНИЕ РАБОТАЕТ (проверено вживую)
Замир оплатил 299₽ → бэк `periods=2` → `/check` вернул `plan_days=180` → после перезагрузки кабинета
появились дни 91–180. #37 закрыт. «90 дней» до перезагрузки был устаревший фронт (читает plan_days
только на входе). Мелкое улучшение (опц.): активному юзеру после доплаты показать 180 без ручной F5.

## ⏳ ЖДЁТ ДЕЙСТВИЙ ЗАМИРА (Claude напоминает в начале каждой сессии, пока не закрыто)

> Правило: файлы готовит Claude, ДЕПЛОЙ и БЭКЕНД запускает Замир. Ничего не теряется — список ниже.

**A. Деплой фронта (накоплено, НЕ залито).** Файлы: `app-v2/cabinet.html`, `app-v2/index.html`,
`app-v2/v2-app.js`, `ingredients_kbju.json`, `version.json` (сейчас `2026-07-12_235737`). Содержит:
#38 (свайп ленты дней больше не зачёркивает), #41 (текст «4000+»→«1500+», оба файла),
#39 (кабинет: подсветка кнопок активности возвращается после F5 — персист сырых data-key в exclusions).
P2a (копирайт), P4a (XSS-фикс, оба файла), #19 рыбный соус→рыба, продление-фронт (guarded, инертен
без бэка), P6e (удалён мёртвый /api-код), P3a (перевзвешивание раз в 30 дней, только cabinet+v2-app.js),
P1 этап 1 (index: тяжёлую загрузку сняли с первого рендера → на requestIdleCallback).
Всё безопасно копится; заливать можно одним разом.
→ ПОСЛЕ деплоя — живые чеки (В13, я из песочницы не могу):
   1) открыть карточку блюда → рецепт раскрывается (P6e: /api/recipe → локальный recipesArray);
   2) P3a: на дне 31/61/91 на «Сегодня» появляется нудж «Прошёл месяц — обнови вес», в профиле
      поля веса разблокированы + кнопка «Обновить вес и пересчитать» → КБЖУ и блюда обновляются,
      прогресс (зачёркнутые дни) сохраняется, после обновления окно закрывается до следующих 30 дней,
      значение переживает F5.

**B. Бэкенд продления (#37).** `yandex-backend/index.js`: колонка `periods` в `subscriptions`
(default 1), `+1` в `handleWebhook` при оплате, отдавать `plan_days = periods*90` в `/check` и
`/login-verify`. Фронт уже читает. Без этого дни 91–180 после доплаты НЕ появятся. После деплоя —
проверяем рост вживую вместе.

**B2. Бэкенд P3a — вес не сохраняется на залоченном профиле (Замир поймал вживую 12.07).**
`handleProfileSave` при `wasLocked` игнорирует FIXED-поля, включая `current_weight/desired_weight` →
новый вес откатывается. Фикс: `FIXED_FIELDS = ['gender','height','age']`, а вес и желаемый вес
перенести в `EDITABLE_FIELDS` (30-дневный гейт держит фронт). Детали — в ИНСТРУКЦИЯ Шаг 2.0.
Фронт корректен — блокирует только бэкенд.

**B3. Аналитика (P6d) — код готов, ждёт ID.** Opus-агент встроил Яндекс.Метрику в оба html (счётчик
+ цели `pay_click`/`plan_fixed`/`weigh_in`/`login_ok`, всё в guard, инертно при `METRIKA_ID=0`). Замиру:
создать счётчик на metrika.yandex.ru → вставить номер вместо `0` в `var METRIKA_ID = 0;` в ОБОИХ файлах
(cabinet.html + index.html) → залить. Бэкап: `backups/snapshot_metrika_20260713/`.

**C. Инфра-части из аудита (решения приняты, за Замиром):**
- P0 (#25): закрыть бакет рецептов приватно + `/recipes-url` первичным.
- P2a (#27): на бэке `ACCESS_DAYS = 95` (доступ ~90 дней + буфер), чтобы копирайт «90 дней» был честным.
- P4b (#31): рейт-лимит `/login-verify` на бэке (email+код брутфорсится).
- P6a (#36): CDN + домен.

_Когда пункт закрыт — Замир говорит, Claude вычёркивает отсюда._

## Журнал сессий
- **29.07 — новый режим «🎬 20 минут с Романом» (третья вкладка рядом с Дома/В зале).** Follow-along
  видео-тренировки для дома (изменённый дом). 6 исходников 1080p из Даунлоадс → сжаты в 480p СО ЗВУКОМ
  (веб): roman_warmup (628с) + 5 основных roman_w1..w5 (Full Body базовая 350с / На всё тело 632с /
  Кор и пресс 371с / Эффективная 375с / Сложная 1108с). Одна тренировка = разминка + одна основная
  (сменная кнопкой «Заменить», выбор из 5, хранится в prefs.romanW). Плеер — со звуком и контролами
  (не немой луп). Постеры 16:9 из кадра. Итого ~92 МБ, 12 хуков в коде, синтаксис OK.
  Тех.деталь: декод 1080p ~6–8x realtime + жёсткий лимит bash 45с → длинные резал на куски по 200с
  (локально в /tmp) и склеивал `-c copy`; фон (nohup) VM убивает при возврате вызова. PHOTO/VIDEO_VER→20260729.
  ЗАЛИТЬ (обе ссылки). Живую проверку вкладки — после деплоя.
- **28.07 (b) — +7 зальных тяг на спину (альтернативы).** IMG_3286/3285/3284/3283/3281/3280/3282 (порядок по
  скриншоту Замира сверху вниз = его пункты 1–7) → x102–x108, grp:'back', alt:true, gym. Названия: вертикальные
  тяги (широким/параллельным/узким/обратным), Т-гриф (широким/узким), горизонтальная рычажная. Видео + квадратные
  миниатюры. Каталог: 130 упр. (28 зал + 28 дом + 74 alt). Синтаксис OK. Появятся в «Заменить» для спины.
  + **x109** (IMG_3291) «Разгибание рук с верёвкой» — трицепс/руки, alt, gym. Каталог: 131 упр. (75 alt). ЗАЛИТЬ.

- **28.07 — ДОМ полностью на видео: 28 упражнений (реальные бодивейт-видео Романа) + квадратные миниатюры.**
  Источник порядка — `kbju-exercises (1).html` (сегодняшний): 30 домашних по порядку, взял первые 28
  (без «Ходьба на месте» h_march и «Приставные шаги» h_step_touch — удалены из EX). Видео — сегодняшние 28 IMG
  из Downloads, порядок по скриншотам (подтвердил визуально: 3240=стульчик №7, 3233=икры, 3246=уголком, 3236=сумо).
  Маппинг IMG→слот проверен монтажом кадров. Все h_ получили видео+квадратную миниатюру (замена AI-фото),
  болгарские/выпады теперь бодивейт. Каталог: 123 упр. (28 зал + 28 дом + 67 alt), 0 битых, синтаксис OK.
  PHOTO_VER+VIDEO_VER→20260728. ЗАЛИТЬ (deploy-v2.sh, --delete чистит старые фото).
  Части 2 (режимы «целая»/«разделённая» — 15-мин видео Романа) и 3 (методика зала от Романа) — ждут файлы Замира.

- **25.07 (f) — ДОМ: 7 слотов на видео (ноги/пресс/кардио), 23 — доснять.**
  h_squat←86, h_lunge←83, h_bulgarian←72, h_step_up←79, h_crunch←89, h_jumping_jacks←85, h_deadbug←87.
  Эти 6 видео убраны из alt (чтобы не дублировались; 87 остаётся и как g_plank_w в зале). Квадратные миниатюры-кадры.
  Каталог: 125 упр. (28 зал + 30 дом [7 с видео] + 67 alt), 102 видео, 0 битых, 0 дублей, синтаксис OK.
  Список 23 на доснять → `_доснять_дом_видео.md` (весь домашний верх + планки + бёрпи и т.п.). ЗАЛИТЬ.

- **25.07 (e) — ЗАЛ пересобран под наши 101 видео: 28 упр. с нашими названиями + квадратные миниатюры из кадров + 73 alt.**
  Замена 28 зальных слотов на видео (умный подбор по группам, согласован с Замиром): жим штанги лёжа(14),
  гантелей(15), наклон(21), разведение(12) / тяга верхнего блока(39), подтягивания(35), гориз.блок(22),
  гантель(37), тяга штанги в наклоне(23) / присед(57), жим ногами(64), сгиб(67)/разгиб(62) ног, икры(69) /
  румынская(68), становая(45), трап(44) / армейский(2), махи(10), к подбородку(9) / сгиб штанга(49), молот(51),
  разгиб блок(78) / абкостер(97), книжка(88), жук(87) / дорожка(92), велотренажёр(94). Названия — наши (из видео),
  id слотов сохранены (day-templates целы). Миниатюры — КВАДРАТНЫЕ кадры из видео (crop min→240, замена AI-фото).
  Остальные 73 видео → alt (place gym/both). ДОМ не тронут (30 h_, фото, без видео — «остальное» потом).
  Правки движка (прошлая сессия) сохранены: pickExercise !e.alt, swapAlternatives/applySwapOne учитывают 'both'.
  Fable-аудит: группы/place/дубли/покрытие/синтаксис — 0 ошибок. Поправил kind заменённых (g_hyperext→compound,
  g_abduction→compound). PHOTO_VER+VIDEO_VER→20260725. deploy-v2.sh: --delete на photos/videos (чистит старые AI-фото
  на бакете) + льёт в ОБА бакета. Синтаксис OK. ЗАЛИТЬ.

- **25.07 (d) — 98 РЕАЛЬНЫХ ВИДЕО тренировок: раскладка + расширение каталога. + Продамус-ссылка.**
  • Все 98 из Downloads сконвертированы (480px, h264, без звука, faststart, ~16МБ) → `_new_videos/`, потом в `workout_videos/`.
  • **Фаза 1:** 29 видео привязаны к базовым 58 (`g_bench.mp4`, `h_squat.mp4`…). Старые заглушки убраны.
  • **Фаза 2:** 69 «лишних» зальных вариаций заведены как упражнения-альтернативы `x01…x98` с `alt:true`
    (grp/place/kind авто-из чертежа). Каталог EX: 58 → **127** (58 базовых + 69 alt).
  • **Движок:** `pickExercise` получил `!e.alt` (×3 фильтра) — альтернативы НЕ идут в авто-подбор дня, методика
    58 базовых цела. `swapAlternatives`+`applySwapOne` учитывают `place:'both'` → «Заменить» отдаёт видео-аналоги
    той же группы (в зале gym+both, дома home+both).
  • **Аудит** Fable-субагентом: 127 упр., 0 дублей id, фильтры методики на месте, все 98 видео привязаны. Блокер
    (x03/x04 «Жим гантелей сидя» chest→shoulders) — исправлен.
  • Продамус-ссылка → `https://payform.ru/m8c6vpk/` (index+cabinet). VIDEO_VER → 20260725.
  • Не-блокеры (на решение Замира): equip у ~15 alt неточен, cue шаблонный, пары дублей-названий (x03/x04, x71/x82),
    вес не показывается при замене на `both` в зале. Синтаксис OK. ЗАЛИТЬ статику (видео 16МБ + workouts.js).

- **25.07 (c) — 🔴 ВХОД СЛОМАН после свапа = CORS гейтвея. + горизонтальный скролл.**
  • **Корень входа:** `api-gateway-spec.yaml` CORS `origin` был ТОЛЬКО `roman-app-v2`. После свапа сайт на
    `roman-nutrition-app` → браузер блокирует ВСЕ запросы к API (вход/профиль/прогресс) → `fetch` падает →
    кнопка «Проверяем» зависала. Исправил origin → `https://roman-nutrition-app.website.yandexcloud.net`.
    ⚠ НУЖЕН РЕДЕПЛОЙ ГЕЙТВЕЯ (Замир). После этого тест-бакет roman-app-v2 потеряет вход (CORS теперь боевой).
  • **Защита от зависания (код):** `loginVerify` обёрнут в try/catch, `doLoginVerify` — `finally` сбрасывает кнопку +
    сообщение «Нет связи с сервером». Даже при сбое кнопка оживает (index.html + cabinet.html).
  • **Горизонтальный скролл:** `html/body { overflow-x:hidden; width:100%; box-sizing:border-box }` (оба файла).
  Синтаксис OK. Заливать: гейтвей (спека) + статика (deploy-v2.sh на боевой).
- **25.07 (b) — Онбординг-гейт витрины + фикс «съел не фиксирует блюдо».**
  • **«Съел» не фиксировал блюдо:** `swapNext/swapPrev` блокировались только замком (`isCardLocked`), а «съел» ставит
    `frozen`/`_eatenCards` — свайп это не проверял → съеденное всё равно листалось. Добавил гейт `isCardEaten` в оба
    (`index.html`+`cabinet.html`). Кабинет: «съел» уходит на сервер (`toggleMealEaten→saveProgressDebounced`) и
    восстанавливается; витрина — только в памяти (аккаунта нет).
  • **Онбординг-гейт витрины (`v2-app.js`):** пока нет данных (нет КБЖУ) — доступен только «Профиль» (подсвечен
    «дышит», `.gate-hi`), остальные табы приглушены + 🔒 (`applyGate` теперь работает в `V2_STORE` по `state.kcal>0`).
    На «Сегодня» карточки Меню/Тренировка/Покупки приглушены (`.locked-card`+🔒), клик уводит в «Профиль». После
    «Подтвердить» (`renderMeals → window._v2Refresh`) — гейт снят, короткая вспышка табов (`.unlock-pulse` ~2с),
    замки убраны, клик работает. Синтаксис OK. ⚠ UI глянуть живьём.
- **25.07 — Сплэш-экраны, вход-хореография, золото кнопок; ⚡ БОЕВОЙ ТЕПЕРЬ ТОЖЕ V2 (указание Замира).**
  • **Сплэш index.html**: самодостаточный блок после `<body>` (стиль+разметка+скрипт, маркеры СПЛЭШ//СПЛЭШ):
    тёмный экран «РГ + Формула здорового тела + Загрузка», прячется по DOM+баннер (мин 500мс, макс 6с),
    затем «сборка» интерфейса классами `html.app-enter/app-entered` (баннер, .wrap, feedbackBtn). Вариант
    с CSS-шаром-«Сатурном» показан Замиру и ОТКАЧЕН по его решению («только загрузка»).
  • **Сплэш cabinet.html**: тот же блок, но держится ДО ЗАГРУЗКИ ДАННЫХ КЛИЕНТА — скрытие по
    `window.__cabinetReady()`, который зовёт `initCabinet` в 2 точках: после `await enterCabinet()`
    (данные+прогресс встали, fixBtn спрятан) и перед `openLogin()` (нет сессии). Аварийный макс 12с.
    Смысл: клиент не видит «прыгающую» страницу и кнопку «Зафиксировать план» те ~6с, пока тянутся его данные.
  • **Кнопки**: `.calc-btn/.buy-btn/.pay-continue-btn/.act-confirm` в ОБОИХ html переведены с блеклого
    `#EFDDB0` на градиент лендинга `#B9903E→#E9CA6E→#F9ECB8` + автоблеск `btn-sheen` (в V2-слое, `!important`).
    `.buy-btn.closed` оставлена серой (явный override). Формулы/движок НЕ тронуты.
  • **Кнопка активности — состояние**: `confirmActivity()` и `markSavedActivityConfirmed()` ставят на
    `.act-confirm` текст «Подтверждён ✓», `toggleActBtn()` возвращает «Подтвердить» при смене выбора (оба html).
  • **⚡ ДЕПЛОЙ**: по прямому указанию Замира «сделай сразу на боевом» v2 залит и на `roman-nutrition-app`
    (dish_photos там уже были, 4385 шт), и на тестовый. Проверено agent-browser: сплэш кабинета → растворение
    над формой входа; «Подтверждён ✓»/сброс — работают живьём; консоль чистая.
  • Попутно: авто-бэкап `~/auto-backup-nutrition.sh` был мёртв с 23.06 (стухший index.lock + старый путь
    ~/Desktop/nutrition-app) — починен (путь → ~/Claude/Projects/nutrition-app), месяц работы закоммичен в
    `/Users/cosmos/Backups/nutrition-app.git`.
  • **Скин С2-голд (просьба Замира «все блеклозолотые цвета — таким золотым, обводки/рамки/цифры везде»)**:
    глобальный sed по 6 файлам (index/cabinet/admin.html, v2-app/v2-ui/workouts.js): `#C8B080→#E9CA6E`,
    `#EFDDB0→#F5D57A`, `rgba(216,192,138→rgba(233,202,110`, `rgba(239,221,176→rgba(245,213,122`; токен
    `--border` поднят до `.45`. Детали и откат — в `ТЕМЫ.md` (секция С2-голд). Задеплоено на ОБА бакета.
- **16.07 (9) — 🔴 КОРЕНЬ дрейфа недель РЕШЁН: неделя тренировок = функция от зачёркнутых дней. + методичка, + тексты.**
  • **#1 Неделя тренировок (корень).** Симптом: в превью висела неделя 11, «Сделано» гнала 11→12; недели
    брались из застрявшего `localStorage.v2_wk_week`. РЕШЕНИЕ (доработка идеи Замира): неделя — ЧИСТАЯ функция
    `1 + floor(зачёркнуто_дней/7)`. Источник — `window._crossedDaysCount()` (хост, из `dayStates`, сервер-
    авторитетно). Локального счётчика недели БОЛЬШЕ НЕТ → весь класс багов (11/32/33, ре-заливка) устранён в корне.
    В превью зачёркивание заперто → 0 → неделя 1. Кнопку «Сделано» убрал, поставил «?» с пояснением
    (`_wkWeekInfo`). Файлы: `workouts.js` (weekCounter/currentWeek/setWeekCounter, render, info-кнопка, CSS),
    `cabinet.html` + `index.html` (`window._crossedDaysCount`). `markDone`/`v2_wk_week` — мёртвый код, безвредны.
  • **#2 Методичка тренировок** — создан `ТРЕНИРОВКИ_методика.md` (полное описание для Замира/Романа).
  • **#3** Разминка: убрал «— и дома, и в зале» из фразы.
  • **#4** пусто. **#5** обрыв («после слова в Профиле…») — ждём продолжение от Замира.
  Синтаксис OK (3 файла). ⚠ Живая проверка недели после деплоя: превью → неделя 1; в кабинете зачёркивание
  7 дней → неделя 2.
- **16.07 (8) — ОТКРЫТОЕ ПРЕВЬЮ = копия кабинета, заперта на Дне 1. ЗАЛИТЬ статику + ⚠ ЖИВАЯ ПРОВЕРКА после деплоя.**
  Модель (решение Замира): превью — полная копия кабинета, все вкладки работают, но только День 1. День 2+ или
  зачёркивание Дня 1 → окно оплаты. Оплата → Продамус письмом шлёт ссылку на отдельный сайт-кабинет (90 дней).
  Кто хочет вечно листать День 1 бесплатно — пусть; заплатит тот, кто может. Реализация (index.html, всё под
  `!state.purchased` — оплаченный флоу не тронут):
  • **Сетка дней 1–90 в превью:** `renderMeals()` показывает `days-section` + `renderDaysGrid()`. День 1 активен
    (меню в `meal-section` выше), дни 2–90 — класс `.locked` + 🔒.
  • **Гейт:** `toggleDay(d)` — неоплачено && d>1 → `showPayment()`; d===1 → прокрутка к меню. Долгое нажатие
    (зачёркивание) в превью → `showPayment()`.
  • **Тренировки (#1):** блок «🔒 ещё N тренировочных» убран, `visibleDays=wk.days` — показаны все дни недели.
  • **Список покупок:** бегунок виден и неоплатившему (старт 1 день), «ОК» при N>1 → `showShoppingPaywall(n)`
    («в полной версии — список на N дней» + оплата); список всегда на 1 день до оплаты.
  ⚠ Синтаксис OK, но UI структурный — НУЖНА живая проверка после деплоя (ИМПРУВЕР В13): День 1 работает,
  День 2 просит оплату, сетка/список/тренировки выглядят правильно. Не смог проверить живьём — сайт на старом коде.
- **16.07 (7) — Тренировки: журнал веса (#1), замена упражнений (#6), черепашка (#5), каркас разминки (#4). ЗАЛИТЬ статику.**
  Всё в `workouts.js` (+ ключи синхронизации в `cabinet.html` `_collectProgress`):
  • **#1 Архив рабочего веса.** Новый `v2_workout_weight_log={exId:[{t,w,reps}]}`. При сохранении веса пишем запись:
    повторы = поле «повт.» (необязательное, выбор Замира «оба») ИЛИ предписанный диапазон недели. Кнопка «📈 История
    рабочего веса» в окне упражнения → лента дата·вес·повторы (скролл). Ключ уходит в облако.
  • **#6 Замена упражнения.** Кнопка ⟳ у названия в окне упражнения → меню альтернатив той же `grp` и `place`.
    `v2_workout_swaps={slotId:altId}`, одношагово по id слота → обратимо (выбрать исходное = снять). `buildWeek`:
    `applySwapOne` перед `applyVariant`, `used.add` от повторов. Ключ в облако.
  • **#5 Черепашка.** Кнопка 🐢 в фото-обёртке окна (видна только при наличии видео) циклит playbackRate 1×→0.5×→0.25×.
  • **#4 Разминка (каркас).** Синтетическая карточка «Разминка» ПЕРВОЙ в каждом дне (дом и зал): 5–15 минут,
    «в блоке разминка несколько упражнений». Тап → почти полноэкранное окно с крестиком и лентой видео
    `warmup_videos/warmup_1..24.mp4` (грациозно скрывает отсутствующие). ⏳ ЖДЁМ видео от Замира.
  • **Видео упражнений:** Замир пришлёт видео всех упражнений (заменить фото) + для замен + разминки. Механизм показа
    видео вместо фото уже есть (#43: где есть `.mp4`, показывается видео).
  • **#2 Витрина-отклонения — РЕШЕНО через ОТКРЫТОЕ ПРЕВЬЮ (решение Замира, разворот P0).**
    Корень: превью грузило `demo-recipes.json`=72 блюда → мотору нечем добить день до ±3% при листании.
    Сначала сделал рефрейм-в-продажу (плашка), но Замир выбрал ОТКРЫТЬ превью полностью:
    – **`recipes.json` снова ПУБЛИЧНЫЙ** (`deploy-v2.sh`: ACL `private`→`public-read`) — осознанный разворот P0;
      вся база (3736 блюд + КБЖУ + инструкции) снова бесплатно доступна. Гейт `/recipes-url` остаётся рабочим,
      просто больше не обязателен. Плата держится на 90-дневном плане, тренировках, списке покупок, обновлениях.
    – **index.html:** превью грузит `/recipes.json` (все 3736; фолбэк на demo), мотор балансит день как в кабинете.
    – **`_applySwapDOM` (2 бага починены, актуальны и для кабинета):** «вернуть в норму» → `compensateDaySwap` →
      перерисовка ИЗ КЭША (превью → rebuild `#mealsContainer` из `dayMenuCache`; день → `renderDayMeals`) → тост
      ПО ФАКТУ пересчёта (`_macroDeviations`), а не безусловно по клику; если вне нормы — реальные числа + повтор.
    – Рефрейм-плашка `_showPreviewBalancePromo` удалена (не нужна на полном наборе).
    – Лимит бесплатных замен СНЯТ (Замир): в `swapNext` убрана проверка `SWAP_LIMIT_FREE` — листаем безлимитно.
      `showSwapLimitPromo`/`SWAP_LIMIT_FREE` остались как мёртвый код (не вызываются).
    🛡 ИМПРУВЕР: это отменяет P0 (задача #25). НЕ регрессия, а продуктовое решение Замира — зафиксировано осознанно.
  Синтаксис OK (workouts.js, cabinet.html). ⚠ Требует деплой статики (bump версии подтянет на устройства).
- **16.07 (6) — 🔴 DURABLE admin-СБРОС: маркер `reset_at` + рукопожатие `reset_ack`. ЗАЛИТЬ БЭК + статику.**
  Проблема (Замир): admin-сброс чистил сервер, но уже ОТКРЫТАЯ сессия клиента (с неделей 11 в localStorage)
  заливала старое обратно → сброс «не держался» ни в браузере, ни в PWA. Решение (устойчиво к ре-заливке):
  • **Бэк `handleAdminReset`:** вместо `DELETE user_progress` пишет МАРКЕР `dbSaveProgress(email,{reset_at:now})`
    (профиль по-прежнему удаляется — сброс профиля/исключений работает).
  • **Бэк `handleProgressSave`:** РУКОПОЖАТИЕ. Если в строке уже стоит `reset_at`, сохраняет его в новом блобе,
    ПОКА клиент не пришлёт `reset_ack ≥ reset_at`. Значит открытая вкладка со старой неделей НЕ затирает маркер
    (её ack устаревший) — маркер доживёт до первого клиента, который его применит.
  • **Клиент `_collectProgress`:** добавляет `reset_ack = localStorage.v2_reset_applied` в каждый сейв.
  • **Клиент `restoreProgress`:** в начале — если `p.reset_at > v2_reset_applied` → стереть ВСЁ локальное
    (тренировки/дни/съел/замки), записать эпоху `v2_reset_applied=reset_at`, отправить подтверждающий сейв
    (снимет маркер) и выйти (старую неделю в блобе игнорируем). Срабатывает на следующем ОТКРЫТИИ приложения.
  Итог: после admin-сброса клиент при перезагрузке гарантированно обнуляется и НЕ воскрешает старое.
  ⚠ Требует НОВЫЙ клиент на устройстве (детекция маркера) — старый cabinet.html маркер игнорирует. Деплой
  бампает версию → PWA подхватит. Синтаксис OK (index.js + cabinet.html). ИМПРУВЕР: В2 (облако-авторитет) + новый
  урок «сброс должен переживать ре-заливку открытой сессии → маркер+ack, а не просто DELETE».
- **16.07 (5) — Открытие на «Сегодня» + замок исключений (защита от удаления). ЗАЛИТЬ статику.**
  • **Открытие на «Сегодня» (v2-app.js):** при старте залогиненного показываем СРАЗУ вкладку «Сегодня»
    (`show('today')` перед `applyGate(true)`) — раньше applyGate для готового профиля не активировал
    вкладку → пустой синий экран, пока не тапнешь (и тап по «Профилю» давал прыжки скролла). Новичка без
    профиля applyGate уводит на «Профиль». (Согласуется с прошлой правкой «после фиксации остаёмся в Профиле».)
  • **Замок исключений — новая семантика (Замир):** замок = ЗАЩИТА ОТ УДАЛЕНИЯ, а НЕ переключатель
    сохранения. Реализовано в cabinet.html:
    – удаление (× на чипе, «сбросить всё») при ЗАКРЫТОМ замке → блок + плашка «снимите замочек»;
    – добавление исключения авто-ЗАКРЫВАЕТ замок (`_sendExclusionFromBox`, кроме «верни всё»);
    – при закрытом замке добавлять МОЖНО, удалять — нельзя, пока не снимут;
    – РАЗВЯЗАНА персистентность от замка: исключения сохраняются ВСЕГДА (`buildProfileFromState`,
      `persistExclusions`, `doRemoveExclusion`/`clearExclusions` теперь всегда persist). Раньше открытый
      замок = сессионно, и снятие ради удаления одного продукта теряло весь список на F5.
  • Синтаксис OK (cabinet.html + v2-app.js). Деплой теперь бампает версию → PWA подхватит.
- **16.07 (4) — 🔴 КРУПНО: авто-обновление PWA было СЛОМАНО → правки с 13.07 не доходили до home-screen юзеров.**
  • Симптом: у Замира в БРАУЗЕРЕ приложение обнулилось, а в PWA (домашний экран) неделя 32 висит даже после
    переустановки иконки. Диагностика: PWA застряла на `?v=2026-07-13`.
  • **КОРЕНЬ:** механизм авто-апдейта (cabinet.html:1716+): при открытии тянет `/version.json`, если версия
    новее `APP_VERSION` — reload с `?v=<версия>` (пробить iOS-кэш). НО: `version.json` был СТАТИЧНЫМ
    (`2026-07-13`), а `deploy-v2.sh` его НЕ бампал (просто заливал тот же) и НЕ подставлял `APP_VERSION`
    (хардкод `2026-07-06`). → PWA один раз перешла на v=13.07 и больше «новой версии» не видела → замёрзла на
    html от 13 июля. ВСЕ фиксы с 13.07 (P0, баги меню/тренировок, сброс, рост/возраст, клетчатка…) до
    PWA-юзеров НЕ доходили. Браузер брал свежий html напрямую (no-cache), поэтому там всё работало.
  • **ФИКС `deploy-v2.sh`:** каждый деплой генерит `TS=$(date +%Y-%m-%d_%H%M%S)` → пишет в `version.json` И
    подставляет в `APP_VERSION` каждого html (sed в temp, оригиналы целы). Теперь PWA при открытии видит
    новую версию → один reload начисто → подхватывает все правки. Синтаксис OK, sed проверен (меняет ровно
    1 строку). ЗАЛИТЬ deploy-v2.sh.
  • 🛡 УРОК: «задеплоил» ≠ «дошло до пользователя». На PWA/iOS проверять, что версия реально бампается и
    клиент перезагружается. Полгода фиксов могли не доходить незаметно (браузер маскировал).
- **16.07 (3) — Тренировки не сбрасывались: сервер был ПЕРЕ-отравлен старой неделей (разобрано, НЕ баг сейчас).**
  • Симптом: у Замира неделя 32/цикл 5 висела после сброса. Диагностика (Chrome, live): и localStorage, И СЕРВЕР
    держали week=32. Т.е. сброс чистил сервер, но клиент заливал старую неделю ОБРАТНО.
  • **Корень:** `workouts.js` LS.set-обёртка (стр.152) на КАЖДУЮ запись тренировочного ключа дёргает
    `window._saveProgress`; а `v2_workout_snapshot` пишется при КАЖДОМ рендере (стр.593). Гейт `_wkCloudReady`
    (cabinet.html 1841) глушит это до конца `restoreProgress`. НО когда Замир впервые сбрасывался — фикс
    очистки-на-пустом-сервере ещё НЕ был задеплоен → снимок залил week32 на сервер до очистки → сервер
    перестал быть «пустым» → фикс больше не срабатывал.
  • **Сейчас всё корректно (код НЕ менял, механизм уже правильный):** порядок в enterCabinet —
    `await restoreProgress()` (1385, гейт ЗАКРЫТ, чистит localStorage при пустом сервере) → потом
    `_wkCloudReady=true` (1399). Проверено ЖИВЫМ тестом: подложил week32 в localStorage при пустом сервере,
    reload → localStorage очистился, на сервер НЕ залилось. ✅ Само-исцеление работает.
  • **Аккаунт Замира почищен** (desktop): снял тренировочные ключи + залил пустой workout-блоб (`_collectProgress`
    без ключей) → сервер `workout:[]`. Его ТЕЛЕФОН само-сбросится при открытии (сервер пуст + фикс живой).
  • Вывод: durable-фикс уже в проде и работает; проблема была разовым pre-fix отравлением сервера.
- **16.07 (2) — Показатель «Клетчатка» убран из UI (решение Замира). ЗАЛИТЬ cabinet.html + index.html.**
  • Причина: цифра «38 г клетчатки/день» (норма пищевых волокон IOM: 14 г/1000 ккал → М<51 38, 51+ 30;
    Ж<51 25, 51+ 21) читалась как «столько добавки-клетчатки досыпать в еду». Это была суточная НОРМА из
    обычной еды, не добавка; «сибирская клетчатка» — отдельный ингредиент, к цифре не относится.
  • Убрана карточка `r-fiber` + расчёт `const fiber` + сеттер — в ОБЕИХ страницах (cabinet + витрина).
    Логику меню НЕ трогали: fiber по-прежнему отслеживается в билдере пресетов (в еде клетчатки хватает).
    Синтаксис OK, висячих ссылок нет.
- **16.07 — РЕГРЕССИЯ рост/возраст = 0 после фиксации. ЗАЛИТЬ cabinet.html + СБРОС затронутых.**
  • **Причина (моя регрессия от переноса стажа в блок фиксации):** `selectTrainingExp` (cabinet.html:2162)
    делал БЕЗУСЛОВНЫЙ авто-сейв профиля при выборе кнопки. Пока стаж был отдельной карточкой НИЖЕ блока —
    его жали после ввода роста/возраста, сейв уходил с верными данными. После переноса стажа ВНУТРЬ блока
    фиксации человек жмёт стаж ДО ввода роста/возраста → сейв уходит с `height:0/age:0`, а бэкенд
    `handleProfileSave` ЛЮБОЕ сохранение делает `locked=true` (`dbSaveProfileRow(...,true,...)`) → профиль
    залочен с нулями → настоящая фиксация FIXED_FIELDS (gender/height/age) уже НЕ пишет → нули навсегда.
  • **Фикс (клиент):** авто-сейв в `selectTrainingExp` теперь только при `state.profileLocked` (до фиксации
    стаж сохранит сам `confirmLock` — он в blob `exclusions` в `buildProfileFromState`). Преждевременного
    локирования нулями больше нет. Синтаксис OK.
  • **⚠️ Затронутые профили** (онбординг в окно бага + клик стажа до роста/возраста) — залочены с нулями,
    лечатся ТОЛЬКО сбросом (`сброс_клиента.sh` / admin.html) + повторный онбординг.
  • 🛡 **Бэкенд-страховка СДЕЛАНА (решение Замира — «делаем бэк тоже»):** в `handleProfileSave` (index.js)
    `lock = wasLocked || (height>0 && age>0 && gender)` вместо безусловного `true`. Любой ранний сейв больше
    НЕ лочит профиль нулями; раз залочен — остаётся. Синтаксис OK. ЗАЛИТЬ функцию (PRODAMUS_SECRET на месте).
    Теперь защита двухслойная (клиент + бэк).
  • 🛡 УРОК: перенёс поле (стаж) в другой блок — НЕ проверил его собственный обработчик (`selectTrainingExp`
    с авто-сейвом). Импакт-трейс при переносе UI-элемента обязан включать его onclick/обработчики.
- **15.07 (вечер-2) — UI-правки по просьбе Замира + новая admin.html. ЗАЛИТЬ (deploy-v2.sh).**
  • **Открытие дня не «бегает»:** убран `container.scrollIntoView` в `renderDayMealsFromEngine` (cabinet.html
    ~7940) — при открытии дня экран остаётся вверху, на ленте. Скролл к РЕЦЕПТУ (тап по фото, ~8010) и
    переход на секцию дней при входе — НЕ трогали.
  • **Убран авто-переход на «Сегодня» после фиксации/«Подтвердить»** (Замир: оставаться в «Профиле», там
    ещё настройки). Причина: фиксация растянута на 2 шага (Зафиксировать → выбрать активность → Подтвердить),
    флаг `_v2JustFixed` взводился при фиксации и доживал до confirmActivity → срабатывал на «Подтвердить».
    Переход убран в ОБОИХ местах (confirmLock 1548 + confirmActivity 2268), флаг больше не используется.
    🛡 НО был ВТОРОЙ, скрытый переход — в `v2-app.js applyGate` (строка 428: `if(!first) show('today')`):
    гейт при переходе профиля в locked сам уводил на «Сегодня». Импакт-трейс первого захода был неполный
    (искал только в cabinet.html). Убрано и там (16.07) — теперь после фиксации точно остаётся в «Профиле».
  • **admin.html ПЕРЕСОЗДАН** (в репо его не было — жил только в старом бакете roman-nutrition-app, оттого
    «Load failed»: CORS шлюза сужен до roman-app-v2). Новый `app-v2/admin.html` смотрит на актуальный
    API_BASE, две функции (сброс профиля `/admin-reset` + продление `/admin-extend-subs` dry-run/apply),
    ключ не хранится. Добавлен в deploy-v2.sh (HTML-цикл). После деплоя доступен на
    `roman-app-v2.website.yandexcloud.net/admin.html`.
  • **`сброс_клиента.sh`** (корень) — сброс профиля через curl (CORS не мешает): `bash сброс_клиента.sh почта`.
- **15.07 (вечер) — Стаж тренировок → в блок фиксации, на замок (решение Замира). ЗАЛИТЬ cabinet.html.**
  • **Разбор вопроса Замира «стаж ни на что не влияет»:** он прав на 2/3. Маппинг (workouts.js:187-193):
    `gt1`→experienced, `none`→novice, `lt1`→**novice** — т.е. «не занимался» и «до года» дают ОДИНАКОВУЮ
    программу. Влияет только «от года»: другая схема недели (WEEK_PRESETS сплит vs фулбоди), expLvl 2 vs 1,
    и снимается лимит «6 упражнений/день». Методически «оба новички» — это верно, не баг.
  • **Почему замок (и Замир прав):** смена стажа на ходу ветвит схему недели → меняются ключи дней
    (fullA/fullB ↔ upper/lower) → отметки «Сделано» сиротеют, рабочие веса у изменившихся упражнений
    теряются, а счётчик недели (v2_wk_week) остаётся → человек падает в середину программы, которую
    никогда не делал.
  • **Сделано:** карточка `#trainingExpCard` удалена, блок стажа перенесён ВНУТРЬ `#fixedParamsCard`
    (перед кнопкой «Зафиксировать»); замок в `lockFixedFieldsInForm` (как у пола); текст-плашка блока
    обновлена (добавлен стаж); ⚠️ добавлена ВАЛИДАЦИЯ в `onCalcClick` — стаж обязателен ДО фиксации
    (иначе залочился бы угаданный из частоты тренировок). Синтаксис OK, перенос проверен скриптом.
  • **СХЛОПНУТО до 2 кнопок (решение Замира, вариант «б»):** `lt1` «Меньше года / не тренировался» и
    `gt1` «Больше года». Кнопка `none` убрана — она вела в тот же тир novice, т.е. вопрос обещал разницу,
    которой нет. Движок значения не менял: принимает `lt1`/`gt1` И легаси `none`→novice, поэтому у ранее
    сохранённых профилей программа НЕ меняется. При восстановлении легаси `none` подсвечивает кнопку
    «Меньше года» (иначе блок выглядел бы без выбора, при том что стаж залочен). Синтаксис OK.
- **15.07 (день) — ПРЕСЕТЫ ПЕРЕСОБРАНЫ на исправленных данных ✅ ЗАКРЫТО.**
  • **Итог:** 238 якорей — **231 с 30 уникальными днями, 7 с 28**. Дублей блюда в дне **0**, превышений UL **0**,
    блюд не из базы **0**, микро 18.68/20, уник. блюд на якорь 121.7. Было у старого файла: 2 якоря с **1 днём
    на месяц**, 1 дубль, 60 маргинальных UL. Главный выигрыш — минимум 28 разных дней у ВСЕХ.
  • **Почему пересобирали (довод Замира, он был прав):** старый файл собран на СТАРОЙ деке (36 вег-блюд были
    скрыты ложными мясными тегами) и заниженных микро → его выбор дней сделан по неверным данным, сравнивать
    с ним смысла нет. Новый — на исправленных.
  • **Почему первая ночная сборка провалилась (28 бедных якорей):** НЕ данные и НЕ UL (проверял — текущие дни
    проходят UL-фильтр, 0 отказов). Причина: (1) я дал НЕПОЛНЫЙ конвейер — только билдер, без `fix_poor_anchors.py`
    (он существует ровно для экстремальных профилей); (2) 120 сек/якорь на 1 ядре — мало.
  • **Что доработано в инструментах:** многопроцессность в `build_super_presets.py` + `fix_poor_anchors.py`
    (`--workers N`, дефолт ядра−2; на M2 Pro 12 ядер → 10 воркеров) — дало возможность дать якорям 1800 сек
    вместо 120; **запрет дублей в fix_poor** (его там не было — возвращал вычищенные дубли); **критерий отбора
    бедных** теперь ловит и бедность по КОМБИНАЦИЯМ ДНЕЙ (`DAYS_THRESH=28`), а не только по блюдам (<40) —
    иначе #189 (120 блюд, но 12 дней) не брался.
  • **Кто эти 7 «на 28 днях»:** очень активные профили — жир прижат к полу 21% ккал, углеводы раздуты до 54–62%
    (у нормальных 30–35% / 30–40%). Собрать 30 разных дней из реальной еды при таком раскладе физически нельзя;
    `fix_poor` даёт им коридор ±5/±8% — безопасно, движок дотягивает каждый день до ±3% под человека.
  • ⚠️ 🛡 УРОК: смоук-тест агента-распараллеливателя ПИСАЛ в боевой `anchors_seeds.json`, отчитавшись «файлы не
    тронуты» — поймано сверкой mtime/хэшей. Отчёт агента о «не трогал» ВСЕГДА проверять фактом.
  • Скрипты: `пересборка_пресетов.sh`, `доделать_якоря.sh`. Отчёты в `_АУДИТ_14.07/`. Бэкапы `_АРХИВ/*_20260715*`.
  • ОСТАЁТСЯ: подчасть M7 («съел» слетает после F5 — нужен пиннинг съеденного как замка).
- **15.07 (ночь-6) — M7 замки + QC-агент + скрипт пересборки. ЗАЛИТЬ статику (cabinet.html).**
  • **M7 (коллизия снек-замков):** замки перекусов ключились по `mealType` ('snack'), а снеков 2-3 → второй
    затирал первый и применялся ко всем. Теперь ключ по слоту `_ovKey(mealType, idx)` = `snack@<idx>` во ВСЕХ
    6 точках (установка/снятие/apply/cook-undo; save/restore — pass-through). + миграция старого ключа 'snack'
    на первый снек-слот. + фикс гонки: сброс кэша дней с замками в конце restoreProgress (список покупок
    пре-собирал дни до прихода замков из облака).
  • **QC-агент (Fable, read-only):** синтаксис 5/5 чист, ключи замков единообразны, #48/#49/M5/сброс/бэкенд —
    OK, блокеров нет. Отчёт `_АУДИТ_14.07/QC_session.md`.
  • **ОСТАЛОСЬ (подчасть M7, делатное — НЕ чинил):** «съел» слетает после F5 + `frozen` не восстанавливается
    после пересборки дня. Причина: съеденное блюдо не снимок-сохраняется как замок → при rebuild слот получает
    другое блюдо → guard снимает «съел». Фикс: пиннить съеденное как замок (снимок + frozen ДО первого
    rebalance в applyLockedDishes). Отдельный аккуратный заход.
  • **Скрипт ночной пересборки:** `пересборка_пресетов.sh` (бэкап → `rm -rf seeds_super` → `build_super_presets.py 120`).
    Утром: super_presets_report.md → validate_presets.py (~60 маргинальных UL = норма) → cp SUPER→anchors → deploy.
- **15.07 (ночь-5) — Данные доведены до пересборки (2 Fable-агента). Бэкапы+ревалидация.**
  • **Микро-база (micronutrients.json 1249→1270):** блюд с дырами 61→6. +2 алиаса, +7 реальных USDA-значений
    (Пшено сухое ×24 рецепта, Чечевица варёная, спельта, темпе, брюссельская, ЦЗ-паста и др.), +12 категор-профилей
    (соусы/фалафель/джекфрут…). Осталось 5 имён на ручной ввод Замиром (вишня сушёная, постный сливочный сыр,
    постные фрикадельки, ньокки, сиб.клетчатка). `micronutrients_per_portion.json` перегенерён (скрипта не было —
    восстановлен из add_recipe.py::per100, 3736 блюд). 🛡 Мина: запись «Пшено» = kbju варёное / micro сырое — 0
    рецептов сейчас, но новый рецепт с «Пшено» получит завышенные микро.
  • **Дубли в дне: 79/79 дней починены** (0 дублей по всем 238×30 подтверждено). **Билдер
    `build_super_presets.py` поправлен** — проверка уникальности id в дне (durable для пересборки).
  • ⚠️⚠️ **ПЕРЕД НОЧНОЙ ПЕРЕСБОРКОЙ (Замиру):** (1) удалить чекпоинты `seeds_super/` — иначе билдер
    пропустит «готовые» якоря, собранные ещё БЕЗ запрета дублей; (2) `validate_presets.py` покажет ~60/7140
    маргинальных UL-превышений (B3 36/35, B9 ~1010/1000, Fe ~36/36) — это ВСКРЫТИЕ реальности на честных микро
    (старые пресеты стояли на заниженных данных), пересборка их закроет сама. Не регрессия.
  • Отчёты: `fix_D4_full.md`, `fix_dups.md` (+ `fix_dups_log.json`, `fix_day_dups.py`). Бэкапы `_АРХИВ/*_20260715*`.
  • ОСТАЁТСЯ 1 код-задача: **M7** (замки перекусов) — не блокирует пересборку, делаю аккуратно отдельно.
- **15.07 (ночь-4) — Данные (2 Fable-агента + мой D2). Все с бэкапом+ревалидацией. ЗАЛИТЬ статику.**
  • **D1 (recipes.json):** 132 мясных блюда (шаурма/лаваши/фарш-запеканки/печёночные торты/крабовые роллы)
    пере-типированы `выпечка_десерт`→`основное_блюдо` (по категории ингредиента, не подстроке). выпечка_десерт
    535→403, основное_блюдо 56→188. КБЖУ/portions/счётчик 3736 — побайтно целы.
  • **D3 (recipes.json):** убраны 36 ложных мясных тегов (маскарпоне/сельдерей→«рыба», яйцо/овощбульон→«мясо»)
    — 36 вег-блюд (омлеты/панкейки) раскрыты вегетарианцу.
  • **D2 (ingredients_kbju.json, мой):** «Сало солёное» (было «специи») и «Свиное сало» (было «масла_жиры») →
    категория «мясо» — теперь исключаются вегетарианцем (была латентная дыра). Бэкап + ревалидация (1288 целы).
  • **D4 (micronutrients.json):** +21 безопасный алиас (точные синонимы) → блюд без микро 117→61. Остаток:
    10 «на решение» (напр. Чёрная фасоль→Красная фасоль — рекомендую), 16 «ручной ввод» (Пшено сухое/Чечевица —
    алиас исказил бы микро в опасную для UL сторону, НЕ трогали). ⚠️ `micronutrients_per_portion.json` (для
    билдера) считался ДО алиасов — перегон только если будем ПЕРЕсобирать пресеты.
  • **D6 (anchors_seeds.json):** дубль оладий fr_19968 (был в 4 днях якоря #189, завтрак+ужин) → ужин заменён
    на fr_216794, день в ±3%. НАХОДКА агента: ещё **79 дней в 54 якорях** с дублем блюда в дне (билдер не
    чекает уникальность id в дне) — список в `_АУДИТ_14.07/fix_D4D6.md`, ЖДЁТ решения Замира (патчить/оставить).
  • Отчёты: `_АУДИТ_14.07/fix_D1D3.md`, `fix_D4D6.md`. Бэкапы в `_АРХИВ/*_20260715*`.
  • ОСТАЁТСЯ код: **M7** (замки перекусов по mealType затирают друг друга при 2-3 снеках; «Съел» слетает F5;
    frozen не восстанавливается) — делаю аккуратно следующим шагом.
- **15.07 (ночь-3) — Партия аудит-починок (cabinet.html, ЗАЛИТЬ статику deploy-v2.sh). Синтаксис OK.**
  • **Полный admin-сброс:** `restoreProgress` теперь авторитетно берёт от сервера зачёркнутые дни, «съел»
    и замки (сброс на всех) — не только тренировки. Безопасно (p==null при обрыве → выходим раньше).
  • **M8 (безопасность вег):** последний фолбэк `pickMealForSlot` брал ЛЮБОЕ блюдо, игнорируя исключения
    (мясо вегетарианцу). Добавлена ступень «чистый ПОВТОР без исключённого» перед крайним фолбэком.
  • **M6:** у `day-btn` не было `data-day` → `renderDayMeals(NaN)` → откат на «День 1». Добавлен `data-day`.
  • **#46 (решение Замира: порог ±3%, флагать сразу):** на ПЕРВОМ построении дня зовём `_showDayKbjuToast`
    с `{silentIfOk:true}` — плашка только при выходе за ±3%, в норме тихо. (Глубже: вег-колода бедна жирными
    блюдами — держим в уме, добор блюд отдельно.)
  • **#R1 (решение Замира: лёгкий совет без слов о микро):** `_checkDayMicroDiversity` — если в дне ≥2 блюд
    с печенью ИЛИ ≥2 с морепродуктами (концентраторы ретинола/селена), мягкий тост «разнообразьте приёмы
    пищи». Без загрузки micronutrients.json, без микро-терминов, максимум раз на день.
  • ОСТАЁТСЯ (следующая партия — ДАННЫЕ, отчёт `_АУДИТ_14.07/agent3_*`): D1 126 мясных блюд типованы
    `выпечка_десерт`; D2 «сало» в словаре не мясное (дыра вегу); D3 40 фантомных мясных тегов скрывают
    36 вег-блюд; D4 47 имён нет в micronutrients.json → 117 блюд теряют витамины; D5 энергобаланс/супы;
    D6 якорь #189 дубль оладий. + M7 замки перекусов/«съел»/frozen.
- **15.07 (ночь-2) — Тренировки: дубли дней (M5) + сброс не стирал тренировки (B). ЗАЛИТЬ статику.**
  • **M5 (workouts.js):** id дня был `place+key`, а недельный пресет размножает ключи (upper/lower/upper/lower)
    → дни 1 и 3 имели один id → «Сделано» на одном закрывало оба, неделя из 4 тренировок засчитывалась за 2
    (прогрессия бежала вдвое — отсюда «неделя 32/цикл 5» у Замира). Фикс: id включает индекс позиции
    (`place+i+key`) в render (752) И в счётчике недели markDone (537). Старые отметки текущей недели
    разово сбросятся (id сменился) — не регресс.
  • **B / L-0018 (cabinet.html):** admin-сброс чистит user_progress на СЕРВЕРЕ, но `restoreProgress` был
    чисто аддитивным → тренировки (и crossed/eaten/locks) жили в localStorage и не стирались на клиенте.
    Фикс: `apiGetProgress` теперь отдаёт `null` при ошибке сети/сервера (а `{}` — только при реальном
    успешном пустом ответе), а `restoreProgress` при успешном ПУСТОМ ответе обнуляет ключи тренировок
    `v2_workout_*`/`v2_wk_*` на клиенте. Безопасно: при обрыве связи (null) выходим раньше — чужой прогресс
    не трём. Неделя 32 у Замира сама сбросится после деплоя+перезагрузки (его user_progress уже пуст).
  • ⚠️ ОСТАЁТСЯ: тот же аддитивный разрыв у crossed-дней/eaten/locks — на сброс они пока НЕ чистятся на
    клиенте (только тренировки). Расширить тем же паттерном по решению Замира.
- **15.07 (ночь) — admin.html «Load failed»: причина = CORS.** Страница `admin.html` лежит на СТАРОМ домене
  `roman-nutrition-app`, а CORS шлюза сужен до `roman-app-v2` (во время P0) → браузер блокирует запрос.
  Бэкенд/ключ исправны (сброс профиля `meforpeace@icloud.com` прошёл через curl). ⚠️ ПРИ СВАПЕ v1→v2:
  admin.html должен оказаться на v2-домене (или добавить v1 в CORS шлюза). До свапа сброс — только curl
  на `/admin-reset` (`{admin_key,email}`).
- **14.07 (вечер) — Аудит 5 субагентов Fable + починки партиями (полная сводка: `_АУДИТ_14.07/СВОДКА.md`).**
  • Аудит подтвердил: пересборка пресетов НЕ нужна (веса живые), микро-UL в пресетах 0 (билдер отсекает).
  • **CRITICAL безопасность (ЗАЛИТЬ бэкенд):** C1 — `/debug-last` и `/check?debug=webhook` закрыты за `admin_key`
    (раньше отдавали PII+код входа без авторизации). C2 — webhook fail-CLOSED: без env `PRODAMUS_SECRET`
    теперь отказ 503 (раньше пустой секрет = любой POST давал бесплатную подписку). ⚠️ ПЕРЕД ДЕПЛОЕМ бэка
    убедиться, что `PRODAMUS_SECRET` выставлен в env функции, иначе webhook начнёт отклоняться.
  • **#48+#49 переделаны в ПРАВИЛЬНОМ месте (`v2-app.js`, не cabinet.html).** 🛡 L-0017: мой первый фикс #49
    ушёл в скрытую сетку cabinet.html; видимые чипы дней — `renderFoodDays` в v2-app.js. Причина обоих багов:
    фоновый интервал 4с пересобирал ленту через innerHTML → (#48) сбрасывал подсветку на «сегодня», (#49) рвал
    чип посреди нажатия → таймер дожигал зачёркивание. Фикс: слежу за просматриваемым днём (`_v2ViewDay`),
    не пересобираю ленту без изменений (подпись) и во время удержания (`_v2Pressing`), + защита свайпа
    (8px + scroll-cancel) на чипах. Старый фикс в cabinet.html оставлен (безвреден, скрытая сетка).
  • **M3 (#45 на витрину, ЗАЛИТЬ):** снятие стретча для выпечки перенесено в `index.html` (было только в cabinet).
  • ОСТАЁТСЯ из аудита (следующие партии): M4 #46 (плашка+вег-жир), M5 тренировки дубли dayId, M6 dataset.day,
    M7 замки перекусов/«съел»/frozen, M8 фолбэк игнорит исключения; данные D1–D6 (мис-теги выпечка_десерт,
    сало, фантомные мясные теги, 47 имён в микро-базе); R1 суточный UL на ручных заменах. Minor ~20.
- **14.07 — Порции выпечки + UI дней (по отчёту Замира на персонаже 63→73 вег).**
  • **#45 (порции, ЗАЛИТЬ):** оладьи/панкейки/маффины выдавали 420г — reweight тянул к потолку
    основного блюда × стретч +20% набора массы, спец-потолка для выпечки не было. РЕШЕНИЕ Замира:
    убрать стретч для выпечки. Сделано в `portionCorridor` (cabinet.html): если `dish_type` ∈
    {`блины_оладьи`, `выпечка_десерт`} → стретч НЕ применяется, держим научный потолок (муж<70=350г).
    Настоящие десерты внутри выпечка_десерт всё равно режутся спец-потолком 100–140г, сладкие
    запеканки — фикс-вес (стретч их не касался). Синтаксис инлайн-JS проверен (node -c) OK.
  • **#49 (UI, ЗАЛИТЬ):** кнопка дня зачёркивалась при листании. Длинное нажатие (750мс) теперь
    отменяется ещё и по ЛЮБОЙ прокрутке (window scroll capture) + порог сдвига 10→8px. Раньше
    ловился только pointermove>10px, медленный скролл проскакивал.
  • **#47 (дубли):** ОТВЕТ (без правок, решение Замира — оставить как есть): точный дубль блюда в
    дне исключён (usedIds + 3 дня), повтор dish_type (оладьи+блины) только штрафуется, не запрещён.
  • **#46 (первое меню без плашки):** ПОДТВЕРЖДЕНО — `_showDayKbjuToast` зовётся только после действий
    (свайп/съел/смена/вернуть-в-норму), на ПЕРВОМ рендере дня валидации/плашки НЕТ. Если стартовое
    меню вне КБЖУ — пользователь не увидит. Предложен фикс (ждёт решения): показывать плашку на первом
    рендере ТОЛЬКО если день реально вне ±3% (тихо, если в норме). Плюс проверить, реально ли вег-дни
    промахивают жир (−7%) или это артефакт несимметричного свайпа туда-обратно.
  • **#48 (тап дня откатывает на первый незачёркнутый):** локализовано (toggleDay игнорит used-день),
    вероятно следствие #49 (тап-мисфайр). ЖДЁТ живого ретеста после деплоя #49.
- **13.07 (вечер-2) — Продление существующих клиентов до 90 дней ✅.**
  • Находка: `expires_at` в `subscriptions` хранится ФИКСИРОВАННО (дата оплаты + ACCESS_DAYS в момент
    оплаты). Смена ACCESS_DAYS 31→95 влияет только на будущие оплаты; старые клиенты остались на 31 дне.
  • Веб-консоль YDB не открывалась (TLS: `certificate is not a CA` — и в инкогнито тоже, т.е. не расширение,
    а сама консоль). Обошли через бэк.
  • Сделан разовый эндпоинт `/admin-extend-subs` (index.js + путь в gateway): admin_key, DRY-RUN/apply,
    правило `expires_at → paid_at + ACCESS_DAYS*periods`, но только если больше текущего (не укорачивает,
    идемпотентно), отчёт «было→стало». Задеплоено Замиром (403 без ключа = смонтировано верно).
  • **Применено: 9/9 подписок продлены.** 8×1-период (31→95 дней, +64 дня), 1×2-периода
    (`meforpeace`, +16 до paid+190). Все даты выросли, никого не укоротило. ⚠️ Эндпоинт временный —
    после запуска можно убрать `/admin-extend-subs` из index.js и gateway.
- **13.07 (вечер) — P0 ЗАКРЫТ ПОЛНОСТЬЮ ✅ + видео упражнений (#43) механизм готов.**
  • **P0 финал:** бэк задеплоен (presign `/recipes-url` работает), CORS бакета выставлен, `recipes.json`
    приватизирован (`p0-privatize.sh`: пообъектные ACL, бакет→private, убрана публичность бакета из
    `deploy-v2.sh`). 🛡 ЛОВУШКА (L-0016): публичный ACL МАСКИРОВАЛ отсутствие IAM-прав — после
    приватизации presign отдал `403 AccessDenied`, т.к. SA `nutrition-functions` (владелец `S3_KEY_ID`,
    ключ `YCAJEBb854ceijzr2csiEwOj7`) не имел `storage.viewer`. Замир выдал роль `storage.viewer` на
    каталог → починилось. **Живая проверка:** подпись→200/3736 рецептов, `recipesArray=3736` на свежей
    загрузке, прямая `/recipes.json`→403, витрина(demo)→200. Гейт работает end-to-end.
  • **#43 видео:** попап упражнения играет `workout_videos/<id>.mp4` (muted/loop/playsinline/autoplay,
    poster=фото, тап=закрыть); нет файла → тихо фото. Превью списка НЕ анимируется (Замир: нужна только
    соответствующая фотка) — фото упражнения = первый кадр видео (для g_leg_ext заменил, старое в `_АРХИВ`,
    поднял `PHOTO_VER`→20260713). 🛡 L-0015: iOS `preload=none` тупик play()/loadeddata (в списке успел
    проявиться до отката анимации). Тест-ролики: `g_leg_ext.mp4` (девушка встаёт, 79КБ, из `girl_standup.mp4`),
    `g_bench.mp4` (анимация из 2 фото). ⚠️ ПЕРЕД СВАПОМ v1→v2: заменить/удалить тест-заглушки видео.
    Формат подтверждён: реальное 2-сек видео ~160КБ, грузится мгновенно. Ждём 58 роликов Романа.
- **13.07 (P0 — закрытие платного контента; фронт залит, бэк-код готов).**
  • **Часть A (фронт, ЗАЛИТО, версия 142722):** кабинет `loadNutritionData` → через `fetchRecipesGated`
    (подписанная ссылка), убран публичный дубль `/recipes.json`. Витрина грузит публичный образец
    `demo-recipes.json` (72 блюда, 163КБ) вместо всех 3736. `demo-recipes.json` создан + в deploy-v2.sh.
    Проверено вживую: кабинет и витрина грузятся ✅.
  • **НАХОДКА:** `/recipes-url` РАНЬШЕ НЕ существовал (ни в функции, ни в шлюзе) → гейт всегда падал в
    публичный fallback. Т.е. рецепты и так отдавались публично.
  • **Часть B (бэк-код, ГОТОВ, НЕ задеплоен — за Замиром):** в `yandex-backend/index.js` добавлен
    `handleRecipesUrl` + `presignGetUrl` (S3 SigV4 presign, без новых зависимостей, гейт по активной
    подписке через authEmail) + S3-env-константы + маршрут. В `api-gateway-spec.yaml` добавлен путь
    `/recipes-url` (GET). Синтаксис OK.
  • **Замиру для P0 (по порядку):** (1) статич. S3-ключ для SA с `storage.viewer` на `roman-app-v2`;
    (2) env auth-функции `S3_KEY_ID`/`S3_SECRET`; (3) деплой auth-функции; (4) обновить API Gateway новой
    спекой; (5) тест кабинета (ссылка с `?X-Amz-Signature`); (6) Часть C — снять public-read с `recipes.json`
    (я подправлю deploy-скрипт лить приватно); (7) финал: прямой `recipes.json` → 403.
  • **Творог fr_900003 «порция белка»:** своё фото есть локально (`dish_photos/fr_900003.webp`), но не
    было залито → плейсхолдер. Скрипт `залить_фото_творога.sh` (заливает его фото; альтернатива —
    фото tvorog5 закомментирована). Не критично.

- **13.07 (живая сессия в Chrome — Метрика, Telegram-отчёт, P1-этап2, #39-фикс).**
  • **P6d Метрика** — счётчик 110644177 встроен (METRIKA_ID в обоих html), 4 цели созданы
    (pay_click/plan_fixed/weigh_in/login_ok), проверено вживую: ym загружается, запросы идут ✅.
  • **P1 этап 2** — витрина не грузит recipes.json (~10МБ) до первого взаимодействия; проверено в
    Chrome (на входе 0 запросов, после клика — грузит, демо строит) ✅. #26 закрыт.
  • **#39-фикс** — `confirmActivity` РАНЬШЕ не сохраняла профиль → подсветка активности слетала после
    F5. Добавил `apiSaveProfile(buildProfileFromState())` в конец confirmActivity (locked+cabinet).
    Замир подтвердил: держится после F5 ✅.
  • **Пометка при повторной оплате** — в баннере «за 2 дня» и оверлее продления: «входите по новому
    номеру заказа». (#42 частично.)
  • **Telegram ежедневный отчёт (#44)** — папка `report-bot/` (index.js + package.json): ОТДЕЛЬНАЯ
    Yandex Cloud Function `roman-report`, читает YDB subscriptions, шлёт в группу
    «Анна, Роман и Zamir» (chat_id -1003911790590) ботом @Roman_Pitanie_bot. Метрики: оплат вчера
    (новые/продления по periods), оплат сегодня, активных, истекают за 3 дня, всего. Таймаут 30с,
    память 256МБ, сервисный аккаунт `nutrition-functions` (ydb.editor), env YDB_ENDPOINT/YDB_DATABASE/
    BOT_TOKEN/CHAT_ID. Работает end-to-end (проверено). Осталось Замиру: таймер-триггер cron `0 7 ? * *`
    (12:00 Алматы). Токен бота — только в env функции, НЕ в коде. Известное огр.: paid_at перезаписывается
    (нет посуточного лога платежей) — при желании добавить отдельную таблицу платежей + правку вебхука.
  версии фронта: последняя залитая с #39/Метрикой — см. version.json. Бэкенды (auth + report) — Замир.

- **12.07 (ЖИВАЯ проверка в Chrome, версия 235737).** Через Chrome MCP на боевом roman-app-v2:
  • `/check` → 200 ok:true, `plan_days:90` → бэк Части 2 (periods/plan_days) залит верно, вход НЕ сломан
    (SQL до кода). При periods=2 станет 180.  • #41: на витрине «1500+», «4000+» отсутствует ✅.
  • #39: `_restoreActivityButtons` на проде, round-trip работает (work_sit+freq_2 подсветились); профиль
    Замира сохранён до деплоя → activityKeys пусты, нужно 1 раз переподтвердить активность. КБЖУ цел (2010).
  • P3a-подсказка + залоченные поля видны в профиле ✅.
  • **#40 диагноз:** на ДЕСКТОПЕ таб-бар идеален (`bottom:0`, зазор снизу=0, 657/657) → баг чисто iOS
    (env(safe-area-inset-bottom)), из десктопного Chrome не воспроизводится. Ждём скрин с телефона для
    точечной правки (НЕ блайндить, L-0002).
- **12.07 (баги с теста Замира: #38 свайп-зачёркивание + бэк Часть 2 залит).** Замир нашёл корень #38:
  свайп-прокрутка ленты дней (палец висит >0.75с, но двигается) считалась долгим нажатием → дни
  зачёркивались. Фикс (оба html): на `pointerdown` вешаем `pointermove`, при сдвиге >10px отменяем
  `pressTimer` (свайп ≠ long-press). version → `2026-07-12_200959`. Бэкенд (index.js) Замир задеплоил:
  ACCESS_DAYS=95, вес в EDITABLE_FIELDS (P3a вес сохраняется — подтвердил ✅), periods→plan_days
  (продление). Осталось живьём: продление 91–180 (реальная 2-я оплата / ручной periods=2).
  Открытые баги с теста: #39 (активность/КБЖУ слетают после F5 — только уровень персистится, не сырые
  кнопки; нужен персист data-key + re-render, живой тест). Пункты 1–2 (переход на «Сегодня») — задумано.
- **12.07 (P1 этап 1 — перформанс витрины; index.html).** version.json → `2026-07-12_173409`. JS — OK.
  НЕ деплоил. Корень: `loadNutritionData()` звалась на верхнем уровне при парсе скрипта и грузила
  recipes.json (~10 МБ) «всегда» — конкурируя с первым рендером витрины (даже анониму на лендинге).
  СТАДИЙНО (L-0002: не рефакторить поток загрузки вслепую без живого теста — на index 3 пути загрузки
  рецептов + 5 гейтов nutritionDataLoaded). Этап 1 (безопасно, поведение то же): вызов был fire-and-forget,
  гейты «не загружено» уже терпят → перенёс старт на `requestIdleCallback` (fallback setTimeout 1200мс).
  Витрина рисуется до старта тяжёлой загрузки; залогиненного `initStorefront` успевает увести на
  /cabinet.html ДО фоновой загрузки → демо-данные ему не качаются. **Этап 2 (осталось, нужен живой тест
  Замира):** полная ленивость — грузить рецепты ТОЛЬКО когда открыто демо-меню (единый `_nutritionPromise`
  + self-trigger в calculate/render), чтобы аноним, не жавший демо, не качал 10 МБ вовсе. Не делал вслепую.
- **12.07 (P3a — перевзвешивание каждые 30 дней плана; cabinet.html + v2-app.js).** version.json →
  `2026-07-12_142805`. JS обоих — OK. НЕ деплоил. Решение Замира: окно по ПРОГРЕССУ плана (дни 31/61/91).
  Мотор `calculate()` НЕ трогал — только зову (он сам читает поля веса, валидирует 30–300/дельту 20,
  чистит `dayMenuCache` и пере-рендерит новые блюда, сохраняя `dayStates`/замки). Реализовано:
  `_p3aCurrentDay/_p3aBlock/weighInEligible` (первый невычеркнутый день → блок = floor((day-1)/30);
  eligible при locked && block≥1 && block>lastWeighinBlock), `applyWeighInUI` (idempotent: разблокирует
  ТОЛЬКО currentWeight/targetWeight, показывает баннер+кнопку), `confirmWeighIn` (пред-валидация веса →
  calculate() → lastWeighinBlock=block → apiSaveProfile → перелочить → тост). Персист (В2/L-0001):
  `lastWeighinBlock` кладётся ВНУТРЬ exclusions-JSON (бэк отбрасывает новые top-level ключи — тот же
  приём, что trainingExp) → переживёт F5/девайс, БЕЗ правки бэка. Хуки доступности: `enterCabinet`
  (после restoreProgress) + `saveProgressDebounced` (после кросса дня). Нудж на «Сегодня» (v2-app.js
  renderToday) ведёт в профиль — инертен на витрине (window.weighInEligible не определён). HTML:
  подсказка fixedParamsNote дополнена формулировкой Замира + баннер #weighInBanner + кнопка #weighInBtn.
  index.html НЕ трогал: витрина уводит оплаченного на /cabinet.html, окно там не сработает (как продление).
  🛡 13-слойный чек пройден: В2 персист✓, В10 пересборка блюд✓, В9 идемпотентность+совместимость с
  продлением (#37: при PLAN_DAYS=180 блоки 3–5 откроются на 121/151/181 сами)✓, В12 тост/баннер/нудж✓,
  В13 живой чек — за Замиром. +урок L-0011 (пиггибэк нового состояния в персистируемый blob).
- **12.07 (P6e — чистка мёртвого /api-кода, ОБА файла).** version.json → `2026-07-12_140726`.
  JS обоих файлов через `new Function()` — OK. Живых `fetch('/api/')` не осталось. НЕ деплоил.
  Импакт-трейс перед удалением (grep всех вызовов): `loadMealPlanFromAPI` — 0 вызовов (мертва);
  `buildMealCardHTML_API` — звался только из ветки `if(_apiPlan…)`, а `_apiPlan` ставит лишь
  удалённая `loadMealPlanFromAPI` → вся ветка мертва (транзитивно). Удалил: `loadMealPlanFromAPI`,
  `buildMealCardHTML_API`, мёртвую ветку `if(_apiPlan…)` в renderDayMeals, блок `/api/ingredients`
  в `loadNutritionData` (реальный источник — локальный `/ingredients_kbju.json`), `/api/recipe`
  setTimeout-фолбэк в `buildMealCardHTML`. ЖИВОЙ `loadAndToggleRecipe`: мёртвый `/api/recipe` НЕ
  вырезал голо, а ЗАМЕНИЛ на локальный `recipesArray.find(id)` той же формы, что кладётся в
  `_recipeCache` (на практике кэш уже наполнен предзагрузкой). `_apiPlan` оставил null-флагом
  (сеттера/ридера нет — инертно, скаттер-правки resets не трогал). 🛡 +L-0010 (как безопасно
  чистить мёртвый код: транзитивная трассировка вызовов + замена живого фолбэка на локальный
  источник, не голое удаление). Живой чек открытия рецептов — за Замиром после деплоя (В13).
- **12.07 (проверка продления + батч2 аудита: P2a + P4a + #19).** Правки в ОБОИХ html.
  Формулу `calculate()` не трогал. JS обоих файлов через `new Function()` — OK. version.json →
  `2026-07-12_134232`. НЕ деплоил (заливает Замир).
  • **ПРОДЛЕНИЕ (диагностика, задача #37).** Бэкенд `handleWebhook` продлевает верно:
    `base = max(now, старый expires)` → дни ДОБАВЛЯются к сроку (не рестарт). НО фронт `PLAN_DAYS=90`
    жёсткий + `currentDay()` = первый невычеркнутый день (прогресс, не календарь) → после доплаты
    время доступа растёт, а КОНТЕНТ плана стоит на 90 днях, дни 91–180 не появляются. Движок
    `_buildDayFromSeed` циклит пресеты `%30` — потянет любой день, упор только в `PLAN_DAYS`.
    Нужно: длина плана растёт с числом оплат (вар. а: бэк отдаёт plan_days/periods; вар. б: фронт
    из accessExpires+plan_start). Требует решения Замира + бэк. НЕ реализовано, вынесено в #37.
  • **P2a — честный копирайт.** Убрал ложное «31-дневный доступ» → «Ваш доступ к меню истёк»
    в оверлее `_showAccessExpiredOverlay` (cabinet, ~1280) + комментарий баннера ~1303. Число дней
    из оверлея убрал совсем, чтобы не обещать конкретику до готовности продления (#37). Остальной
    копирайт уже честные «90 дней». index — этой строки нет.
  • **P4a — Stored XSS в исключениях (ОБА файла).** Свободное имя исключения раньше клеилось в
    `innerHTML` и inline `onclick('${id}')` без экранирования (пейлоад сохранялся в облако, срабатывал
    каждый вход, в т.ч. в сессии тренера). Починил по L-0009: чипы — `esc(label)` + `data-excl-id`/
    `data-pref-key` (esc) + ОДИН делегированный `click` (idempotent-гвард `__exclChipDelegated`),
    без inline onclick; диалог `confirmRemoveExclusion` пересобран через `createElement`+`textContent`.
    Проверено пейлоадом `<img src=x onerror=…>` → показывается текстом, `"` не ломает атрибут. Grep:
    0 старых inline-onclick.
  • **#19 — мис-теги рецептов: данные ЧИСТЫЕ (утечки нет).** Скан 3736 рецептов: **0** с тегом
    «вегетарианское»+мясо/рыба → корень побочки #2 закрыт. Все настоящие мясо/рыба/птица/морепродукты
    классифицированы в справочнике, рантайм-фильтр `recipeCategorySet` объединяет категории ингредиентов
    + теги → ловит всё. 182 «неполных тега» (крабовые палочки→морепродукты, куски птицы) рантайм
    компенсирует, утечки нет. Ложные «мясные» (специи для курицы, приправа для свинины, колбасный
    сыр, куриное яйцо) отнесены верно. Единственный judgment call: `рыбный соус` (3 рецепта) в
    категории `соусы`, не `рыба` — вынесено Замиру на решение. Мутаций данных НЕ делал.
  • **#19 доп. — рыбный соус → рыба (решение Замира).** В `ingredients_kbju.json` категория
    `Рыбный соус` соусы→рыба (макросы не тронуты). Бэкап `_АРХИВ/ingredients_kbju_20260712_before-рыбныйсоус.json`.
    Теперь исключающие рыбу не видят 3 рецепта с рыбным соусом. JSON валиден.
  • **#37 продление — ФРОНТ готов (guarded), решение Замира: «бэк отдаёт периоды».** В cabinet.html
    (только кабинет; index — витрина, уводит на /cabinet.html): `const PLAN_DAYS`→`let` + `_applyPlanDays(d)`
    читает `d.plan_days` (или `d.periods`×90) из `/check` и `/login-verify`, ставит `PLAN_DAYS` кратно 90.
    ИНЕРТНО (остаётся 90), пока бэк не пришлёт поле → 0 изменений сейчас. Живая проверка роста —
    ПОСЛЕ деплоя бэка (совместно). НУЖНО ОТ ЗАМИРА (бэкенд, `yandex-backend/index.js`): считать
    оплаченные периоды и возвращать `plan_days` (90×оплат) в ответах `/check` и `/login-verify`.
    Простейше: колонка `periods` в `subscriptions`, `+1` в `handleWebhook`, отдавать в этих двух ручках.
  • 🛡 импрувер: L-0007 повт→2 (скан по подстрокам дал 3 круга ложняков кур/сельд/карп — пойманы
    верификацией на живых именах). +L-0009 (безопасный ввод свободного текста в DOM).
- **12.07 (батч1 аудита: P3b + P5a + P5b).** Точечные правки в ОБОИХ файлах (cabinet.html +
  index.html) + v2-app.js. Формулу `calculate()` НЕ трогал (КБЖУ-инвариант сохранён).
  Бэкап: `backups/snapshot_20260712_батч1-p5p3b/` (4 файла). `node --check` v2-app.js/workouts.js +
  extract-check inline-скриптов обоих html — OK. НЕ деплоил.
  • **P3b — верхняя граница веса.** В `calculate()` И в `onCalcClick()` (Impact-trace: вторая точка
    валидации, чтобы >300 кг не зафиксировался при `openLockPopup`) добавил
    `if (cw>300||tw>300){alert('Проверь вес (30–300 кг)');return;}` сразу после нижней границы.
    Дельту |tw-cw| (кап 20 кг) не трогал. Оба файла.
  • **P3b — «Написать тренеру».** Плашка `fixedParamsNote` (cabinet, статич.) и `locked-banner`
    (index, динамич. в `lockFixedFieldsInForm`) получили кнопку «Ошиблись в данных? Напишите тренеру»
    → `openFeedback()` (существующий флоу, в модалке уже есть Telegram `t.me/Roman_Gadzhiev_l`).
    Placeholder НЕ понадобился — контакт уже в коде. `<button>` для клавиатурной доступности.
  • **P5a — a11y.** (1) Убрал `maximum-scale=1.0` из viewport (оба) — зум вернулся. (2) Добавил
    `:where(a,button,input,textarea,select,[tabindex]):focus-visible{outline:2px solid var(--accent2)!important;outline-offset:2px}`
    после `*`-reset (оба). `!important` — т.к. точечные `outline:none` на инпутах/кнопках имеют
    выше специфичность; так фокус реально виден, без правки 4 CSS-правил (мин.правка L-0003).
    (3) Клавиатурная доступность кликабельных `<div>` (пол/активность/стаж/фидбек): один
    делегирующий `keydown`-слушатель на document (Enter/Space на `[role=button][tabindex]` → `.click()`,
    нативные button/a/input не трогаются) + JS-пасс `markA11y` вешает `role=button tabindex=0` на
    `.option-btn/.act-btn/#feedbackBtn` (idempotent, полную замену div→button НЕ делал). (4) aria-label:
    иконочные ✕ (глиф-пасс в markA11y), свайп ‹/› (в шаблоне meal-nav), фидбек-кнопка, «?»;
    `<label for>`↔`<input id>` в форме профиля (4 поля), `aria-label` на loginEmail/loginCode/payEmail.
  • **P5b — баги данных.** (1) `renderShoppingList` (оба): ключевание сменил с сырого `ing.id` на
    КАНОНИЧЕСКОЕ имя через `_shopCanonName()` — целое куриное яйцо (все жирности/бренды) → «Куриное яйцо»,
    питьевое молоко → «Молоко»; **белок/желток/меланж и растит./сгущ./кислое/топлёное молоко —
    ОТДЕЛЬНО** (проверено на реальных именах из ingredients_kbju.json). Дубли (яйцо ×3, молоко ×2)
    схлопываются. (2) `fmtAmount` (оба): в гард «шт» добавил `&& !n.includes('белок') && !n.includes('меланж')`
    → белок/меланж теперь в граммах, не «шт». (3) `v2-app.js` `pct`: было `Math.round((day-1)/total*100)`
    (застревал на 99% при 90/90) → `(crossedCount()>=total)?100:Math.round((day-1)/total*100)`.
    Логика всех трёх прогнана harness'ом — сходится.
  • **Чек-лист слоёв:** данные/бэкенд — не трогал (канон-карта только на рендере списка, не в данных);
    хранение — не затронуто; ⚠ кэш/версия — **поднять `version.json`/`?v=`** (правились cabinet+index+v2-app.js,
    force-cache) — за Замиром; деплой — все 3 файла уже в `deploy-v2.sh`; витрина зеркалит (index правлен
    идентично кабинету, где применимо). ⚠ **Живая проверка (В13):** визуально не проверял (file://
    блокируется) — нужна проверка после деплоя: фокус-рамка с Tab, зум пальцами, «Написать тренеру»,
    список покупок без дублей, шт/граммы белка, 100% на завершении.
- **12.07 (каскад «не тренируюсь» + баг таб-бара).** Две задачи по v2.
  • **КАСКАД «НЕ ТРЕНИРУЮСЬ» (trainWeekly===0, тайл `freq_0`).** Корень «чёрного пустого
    блока»: при `trainWeekly===0` `buildWeek` даёт пустую программу, но `render()` в
    `workouts.js` всё равно строил короткую карточку, а остальную высоту экрана занимал
    ЧЁРНЫЙ фон body (`background:#000`, cabinet.html:43) → выглядело как пустой контейнер.
    Фиксы: (a) `workouts.js render()` — при `trainWeekly===0` ранний выход с сообщением
    «😴 Ты выбрал: не тренируюсь…» внутри `.card` (фон ТЕМЫ surface, не чёрный), пустая
    программа/тумблер/баннер не рендерятся; (b) `workouts.js` — новый экспорт
    `window._wkTrainWeekly()` (единый источник частоты для v2-app); (c) `v2-app.js` —
    `applyTrainState()` дизейблит таб «Тренировки» (класс `.notrain`, opacity .35, клик →
    модалка-подсказка, а не переход), вызывается в `build()` и интервале 4с; (d) `v2-app.js
    renderToday()` — карточка «Тренировка дня» на экране «Сегодня» скрыта при
    `trainWeekly===0`. Персистентность общая с профилем (freq хранится там же) → каскад
    переживает F5/девайс и обновляется на лету. Дефолт витрины gym_1→2 → таб НЕ гаснет зря.
  • **БАГ «таб-бар улетает на середину при скролле».** Корень: `#v2-tabbar` = `position:fixed`
    + `transform:translateX(-50%)` + `backdrop-filter:blur` — известная связка, из-за которой
    на МОБ. Chromium/WebKit фикс-слой рассинхронизируется со скроллом (на десктопе НЕ
    воспроизвёл — проверено live через Chrome: offsetParent=viewport, бар пиннится корректно
    и после скролла списка покупок). НЕ правка `window.scrollTo(0,_sy)` (та только в
    weight-save тренировок, к покупкам отношения нет). Фикс (`v2-app.js`): убраны ОБА триггера —
    центрирование без transform (`left:0;right:0;margin:0 auto`), `backdrop-filter` снят (фон и
    так `.97`, блюр не виден). Live-инъекция CSS в бой: бар остаётся `fixed;bottom:0`, ширина
    480, по центру — регрессии нет. ⚠ Сам моб-глюк на десктопе не воспроизводится → нужна
    финальная проверка Замиром на телефоне (В13).
  • **Чек-лист слоёв:** витрина зеркалит (index.html тот же `v2-app.js`/`workouts.js` — оба
    фикса работают и там); бэкенд/данные/хранение не трогал (каскад читает уже сохранённый
    freq); деплой — все 3 файла (`v2-app.js`, `workouts.js`) уже в `deploy-v2.sh`; ⚠ кэш —
    поднять `version.json`/`?v=` (Замир); обычный (тренирующийся) сценарий не задет — при
    `trainWeekly>0` всё как было (ветки под условием ===0). Бэкап
    `backups/snapshot_20260712_каскад-нетренируюсь/` (4 файла). `node --check` v2-app.js +
    workouts.js — OK. НЕ деплоил.
- **11.07 (вечер-8, УМНАЯ ПЕРЕКАЛИБРОВКА мини-мотора активности).** Замир: частота тренировок
  почти не двигала уровень (сидячий+7×=24→всего «лёгкая»; после первого занятия — плато). Задача:
  чтобы частота ОСМЫСЛЕННО двигала один из 4 коэффов. **4 коэффа (1.20/1.375/1.55/1.725) и формулу
  calculate() НЕ трогал; формулу комбинации `Math.max(ptsWork,ptsSteps)+ptsGym` НЕ менял** — правка
  МИНИМАЛЬНАЯ (только data-pts тайлов + пороги scoreToLevel).
  • **Ресёрч:** FAO/WHO/UNU PAL (Wikipedia PAL): офис 1.40–1.69, физтруд/1ч бега 1.70–1.99, тяж.
    физтруд 2.00–2.4. Tudor-Locke шаги: <5k сидяч, 5–7.5k низк, 7.5–10k умерен, 10–12.5k актив.
    Compendium: силовая 3.5 MET (обычная) … ≥6.0 (интенсивная).
  • **НОВЫЕ баллы:** work 4/18/30/44→**0/15/30/45**; steps 2/16/30/44→**0/15/30/45** (параллельны work);
    freq 0/8/10/12/14/16/18/20→**0/5/10/15/20/24/28/32** (линейно +5 до 4×, затем +4 — убывающая отдача).
    Пороги scoreToLevel <12/<26/<44→**<15/<30/<45** (шаг уровня = 15).
  • **Как теперь частота двигает уровень** (матрица base×freq): сидит S(0-2)→L(3-6)→M(7);
    стоя L(0-2)→M(3-6)→A(7); ноги M(0-2)→A(3+); физтруд A всегда (не сломан). Раньше сидячий
    упирался в «лёгкую» при любой частоте, стоячий НИКОГДА не достигал «высокой» — теперь частота
    даёт до 2 ступеней. Сидячий+7 → «средняя» (не «высокая» — по просьбе Замира).
  • **Fingerprint (`../fingerprint_activity_calib.py`, реплика live calc, множители те же, без EAT):**
    демо-сетка×4base×8freq (96 768): уровень сменился у 21.9%, цель КБЖУ у 21.7%, ЯКОРЬ у 15.6%.
    Качество матча профиль↔якорь почти не изменилось: средняя дистанция 0.0509→0.0518, «плохих» 26.4→27.4%,
    макс 0.301=0.301. **РЕГЕНЕРАЦИЯ ЯКОРЕЙ НЕ НУЖНА** (пул и коэффы те же; каждый якорь независимо
    валиден в ±3%, номера тасуются без потери качества — тот же вывод, что вечер-6). Распределение
    (равновзвеш. сетка, НЕ реальная популяция): Низ 3.1→9.4, Лёг 25→22, Сре 34→25, Выс 37.5→43.8%.
  • **workouts.js НЕ затронут:** число тренировочных дней читается по `dataset.key` (freq_N→цифра),
    а не по data-pts → правка баллов на него не влияет (проверено).
  • **Чек-лист слоёв:** бэкенд/данные — не трогал (уровень = 1 из 4 строк, механизм хранения тот же);
    хранение — state.activity сохраняется как прежде; ⚠ уже зафиксированные профили хранят РЕЗУЛЬТАТ
    (строку уровня), ретроактивно не пересчитываются — новая калибровка только для новых расчётов;
    ⚠ кэш/версия — **поднять version.json/`?v=`** (правились cabinet+index, force-cache); деплой —
    оба файла уже в deploy-v2.sh; витрина зеркалит (index правлен идентично). Бэкап
    `backups/snapshot_20260711_калибровка-активности/`. `node --check` OK (inline обоих + workouts.js).
    DOM-рендер не гонял. НЕ деплоил.
  • ⚠ **Развилки Замиру:** (1) сидячий+1-2×/нед теперь «низкая» (было «лёгкая») — физиологически
    точнее и безопаснее (1-2 занятия ≈ +45-85 ккал/сут ≈ PAL 1.25, ближе к 1.20 чем 1.375), но это
    смена поведения для «редких» тренящихся; альтернатива — сделать freq_2=15 (сидячий+2→«лёгкая»),
    щедрее. (2) сидячий+7 упёрт в «среднюю» — оставить (реком.) или пустить до «высокой»? (3) ноги+3
    → «высокая» (1.725): курьер + 3 зала = очень активный, по PAL 1.8-2.0 обоснованно; смягчить до 4×?
- **11.07 (вечер-7, КОРРЕКЦИЯ активности — ОТКАТ NEAT/EAT).** Замир: мотор и 4 коэффа НЕ трогать —
  выбор активности лишь ОПРЕДЕЛЯЕТ человека в один из 4 коэффов (1.20/1.375/1.55/1.725), формулу не меняет.
  Откатил вчерашнюю переписку (NEAT/EAT/новые множители) из бэкапа `snapshot_20260711_222723_активность`.
  Простая версия: в блоке активности строка «🏋️ спорт» → «📅 Сколько раз в неделю тренируешься? 1–7»
  (рекомендация: 2 в мес.1, 3 с мес.2). Баллы тайлов 8/10/12/14/16/18/20 — те же, что вели в те же уровни
  (freq_1=8≈gym_1, freq_4=14≈gym_3, freq_7=20≈gym_5); `data-grp="gym"` сохранён → `confirmActivity`+
  `scoreToLevel`+`actFactorMap` НЕ тронуты, ЯКОРЯ НЕ затронуты. Частота теперь задаёт ЧИСЛО тренировочных
  дней (workouts.js: `trainWeekly` в getProfile, buildWeek циклит сплит до N — раньше «5» было зашито в
  пресет). Частота персистится в `v2_workout_prefs`. Работа/шаги без изменений. Версия поднята. node --check OK.
  ⚠ NEAT/EAT-запись ниже (вечер-6) — ОТМЕНЕНА, оставлена для истории.
- **11.07 (вечер-6, ПЕРЕНАСТРОЙКА АКТИВНОСТИ: NEAT vs EAT — ПРАВКА ЯДРА КБЖУ)** — Разделил учёт
  быта (NEAT → множитель) и тренировок (EAT → отдельные калории), чтобы спорт не считался ДВАЖДЫ,
  и привязал число тренировочных дней к ЯВНОЙ частоте (раньше зашито в пресет: опытный+набор=5).
  • **Ресёрч (ссылки в отчёте чата):** силовая сессия 3.0–6.0 MET (Compendium of Physical Activities;
    круговая 8.0). Множители Mifflin (1.20/1.375/1.55/1.725) ПО ДИЗАЙНУ включают И NEAT, И спорт →
    вынос EAT требует снизить базу (иначе двойной счёт). Дефолт частоты 3 (ACSM: новичок 2–3, средний 3).
  • **Новая модель калоража:** NEAT-множители 1.20/1.35/1.45/1.60 (occupational-PAL, спорт вынесен);
    EAT НЕТТО = (MET−1)·вес·часы·частота/7, MET=5, длит.1 ч (для 80 кг×3/нед ≈ +137 ккал/сут);
    EAT прибавляется ПОСЛЕ дефицита/профицита (eat-back — тренировки фуэлятся, NEAT-дефицит цел).
    Все нижние правила сохранены (пол жира набора 21%, kmin 1500/1200, У≥130/120, потолки).
  • **UI:** из блока «🏃 Какая активность» УБРАНА строка «🏋️ Тренировки/спорт» (gym_0/1/3/5) —
    активность = только работа/шаги. В карточку стажа (cabinet, `trainingExpCard`) добавлен вопрос
    «📅 Сколько раз в неделю тренировки?» 1/2/3/4/5 (дефолт 3). На витрине (index) — отдельная
    карточка `trainWeeklyCard` (стажа там нет). Частота ⚠ ВЛИЯЕТ на КБЖУ (в отличие от стажа).
  • **Файлы/функции:** `cabinet.html` + `index.html`: `actFactorMap` (NEAT), EAT в `calculate()`,
    `trainingKcalPerDay()`, `selectTrainWeekly()` (рычаг → пересчёт КБЖУ без сброса), `confirmActivity`
    (убран вклад gym: `pts=max(work,steps)`), `buildProfileFromState`/`applyProfileToState`
    (`trainWeekly` внутри `exclusions` JSON — бэкенд фильтрует top-level белым списком!), state.trainWeekly=3.
    `workouts.js`: `getProfile` (freq из `state.trainWeekly` 1..5, фолбэк из старого gym для совместимости),
    `daysForCount()` (число дней = частоте, сплит по частоте), `buildWeek` (убран trim gym_3), `FREQ_RU` (1..5).
  • **ИМПРУВЕР — влияние на якоря (В10):** `fingerprint_activity.py` (реплика старой/новой calculate).
    Цель КБЖУ меняется у 98.6% (тронуты множители у всех + EAT), НОМЕР якоря — у 41.8% (freq3;
    34–53% для freq1–5). НО качество матча профиль↔якорь НЕ ухудшилось: средняя дистанция по
    долям Б/Ж/У 0.0445→0.0437, «плохих» матчей (>0.06) 20.1%→20.0%, макс 0.301→0.276. Причина:
    якоря выбираются по РАЦИОНУ+группе (не по абсолютным ккал; план ребалансится под точную цель),
    а рационы сдвинулись слабо. **РЕКОМЕНДАЦИЯ: регенерация НЕ нужна** — сетка покрывает новую цель
    так же. Номера тасуются, но каждый якорь независимо валиден в ±3%. Решение — за Замиром.
  • **Чек-лист слоёв:** хранение — `trainWeekly` в облако через `exclusions` (переживает F5/девайс);
    совместимость — старые профили без поля → фолбэк из gym-выбора, иначе 3; деплой — файлы уже в
    `deploy-v2.sh` (index/cabinet/workouts.js); кэш — ⚠ поднять `version.json`/`?v=` (правились 3 файла);
    бэкенд/данные не трогал; витрина зеркалит. **Бэкап `snapshot_20260711_222723_активность`.**
    `node --check` OK (workouts.js + оба inline-скрипта). DOM-рендер не гонял. НЕ деплоил.
  • ⚠ **Развилки Замиру:** (1) NEAT-множители (реком. 1.20/1.35/1.45/1.60; альт. мягче/жёстче);
    (2) MET 5.0 и длит. 1 ч (альт. 4.5 / 45 мин → меньше EAT); (3) EAT eat-back (после дефицита) vs
    в TDEE (тогда дефицит масштабируется и на EAT); (4) НЕТ опции «0/не тренируюсь» — сейчас минимум 1
    и все получают EAT; сидячий не-тренящийся на дефолте 3 переедает ~+100 ккал → предлагаю добавить
    тайл «0 — только питание» (сохранит семантику старого gym_0); (5) регенерировать якоря или нет (реком. нет).
- **11.07 (вечер-5, витрина = таб-оболочка как кабинет, бэклог #1)** — Витрина теперь показывает
  бар-меню и экран «Сегодня» (как кабинет), а не плоскую ленту. Ввёл «витрин-режим» `V2_STORE` в
  `v2-app.js` (гейт сменён `!IS_CABINET`→`typeof IS_CABINET==='undefined'`; `V2_STORE=(IS_CABINET===false)`).
  Витрина: без локов (`applyGate` → ранний return при V2_STORE), старт на «Сегодня», лента 90 дней
  скрыта (`renderFoodDays`/строй-стрип под `!V2_STORE`), демо-день в «Питании». `storefrontSetup()`:
  тизер+🔒 на «Покупки», пробный баннер «Питания», подсказка «Профиля». Тренировки не трогал —
  workouts.js сам даёт 1-день-превью+CTA (гейт `purchased`). index.html: `id="fixedParamsCard"` на
  карточку пол+параметры, подключён `v2-app.js`. Кабинет 1-в-1 не тронут (все ветки под `if(V2_STORE)`).
  ЧЕК-ЛИСТ СЛОЁВ (В): бэкенд/данные — не трогал; хранение — витрина без логина, состояния не пишет
  (state в памяти, дни только после оплаты); кэш/версия — ⚠ поднять `version.json`/`?v=` (index+v2-app.js
  правились, force-cache); деплой — ⚠ `deploy-v2.sh` должен включать index.html + v2-app.js; auth/CORS —
  витрина без входа, не задета; общий ресурс v2-app.js — кабинет защищён флагом; кросс-проект — н/д.
  Бэкап `snapshot_20260711_215223_витрина-таб`. `node --check` OK. НЕ деплоил. ⚠ ЖИВАЯ ПРОВЕРКА за
  Замиром после деплоя (В13): file:// заблокирован расширением, локально не отрисовать.
- **11.07 (фото упражнений #7)** — Перегенерированы ВСЕ фото раздела «Тренировки»: 58 упражнений
  × 2 позы (A=`<id>.webp` начало, B=`<id>_b.webp` конец) = 116 кадров. Стиль — 3D-анимационный
  (премиум фитнес-app), единый герой = reference-элемент Higgsfield `workout_hero_3d`
  (`e997e1a2-5ca3-44da-9389-ea0e0f38ddf7`), модель `nano_banana_2`, техника строго по «cue»
  (старые фото были с неверными упражнениями). Манифест `app-v2/workout_photos/_photos_manifest.csv`
  (116 строк) + скрипт `app-v2/workout_photos/download_workout_photos_v2.sh` (curl→webp, идемпотентный).
  ⚠ Скачивание и деплой за Замиром (CDN недоступен из песочницы). Кредиты Higgsfield: осталось ~27.
- **11.07 (вечер-4, возраст+стаж в тренировках)** — Добавил ОСИ ВОЗРАСТ и СТАЖ в движок тренировок.
  Ресёрч (ACSM «Exercise and Physical Activity for Older Adults»; NSCA Fragala 2019 «Resistance Training
  for Older Adults»; обзор восстановления PMC10317890; ACSM 2026 update). Пороги: young<50 / middle 50–64 /
  older 65+. `workouts.js`: `ageTier/deloadEveryFor/ageStepFactor`, `age`+`deloadEvery`+`ageTier` в
  `getProfile`; `weightStep` (+возрастной множитель), `automat`/`currentWeight`/`progressionFor`/
  `programStatus`/`suggestHomeVariants` (+deloadEvery), `pickExercise` (+суставно-щадящий: убирает
  `impact:'high'`, у 65+ потолок сложности=норме), `ageHint`-плашка. 3 ударных упр. помечены
  `impact:'high'` (h_mountain/h_jumping_jacks/h_burpee); добавлены 2 низкоударных: **h_march**,
  **h_step_touch** (нужны фото — бэклог #7). СТАЖ: прямой вопрос в онбординге (cabinet, карточка
  `trainingExpCard`, `selectTrainingExp`), маппинг none/lt1→новичок, gt1→продолжающий; фолбэк из частоты
  для старых юзеров. Хранение (ИМПРУВЕР): `trainingExp` внутри `exclusions` JSON (бэкенд белым списком
  фильтрует top-level поля → не трогаем бэкенд). Harness проверил прогрессию (young 60→80/год, older
  60→70.5/год). Бэкап `snapshot_20260711_203427_возраст-стаж`. node --check OK (оба). НЕ деплоил.
  Развилки Замиру — в бэклоге #9.
- **11.07 (вечер-3, cloud-sync тренировок)** — Прогресс раздела «Тренировки» теперь сохраняется в
  облако и восстанавливается (как питание). Правки: `cabinet.html` (`_collectProgress` +поле `workout`,
  `restoreProgress` +возврат 8 ключей и `window._wkRender`, `window._saveProgress` c гейтом
  `window._wkCloudReady`, установка флага в `enterCabinet`), `workouts.js` (`LS.set`→сейв-хук,
  `window._wkRender`). Питание синхронится как прежде. Краевые: пул ДО пуша (гейт), восстановление
  без эхо (`setItem`), офлайн — no-op в try/catch (localStorage = кэш), мульти-девайс last-write-wins,
  сброса профиля нет. Детали и КАРТА РЫЧАГОВ — в бэклоге #8. Бэкап snapshot_20260711_192048.
- **11.07 (решения по тренировкам)** — Замир выбрал НАУЧНО-СКОРР. схему (реализована). Развилки:
  неделя — АВТО (уже так); дом-прогрессия — АВТО-ПРЕДЛОЖЕНИЕ на пике цикла (`suggestHomeVariants`
  в workouts.js, подтверждение кнопкой, дедуп по циклу); смена цели — в nutrition-app ЦЕЛЬ
  ФИКСИРОВАНА раз и навсегда → рычага нет, обработка не нужна (пересчёт агента — мёртвый путь,
  безвреден). ⚠ Для somas цель НЕ фиксирована — учесть там. Виджеты AskUserQuestion у Замира
  визуально глючат, но ответы доходят — вопросы задаём текстом.
- **11.07 (движок тренировок #5+#6)** — Переписал прогрессию в `workouts.js` по
  научно-скорр. спеке (бэкап: `backups/snapshot_20260711_184024_движок-тренировок/`).
  • **Неделя — по выполненным тренировкам** (был баг: считалась по зачёркнутым дням ПИТАНИЯ).
    Новый монотонный счётчик `v2_wk_week`, растёт когда все дни недели отмечены «Сделано»;
    без капа, без %90 → продление плана бесшовно (planDays = только видимость). Убрал
    зависимость от `dayStates`.
  • **Автомат** `automat(ex,W,goal)`: reps R0→Rmax (+1/нед) → пик → вес +ΔW раз в цикл;
    sets +1 раз в 2 цикла (cap 4); deload каждые 6 нед (−подход, повторы к низу, вес держим).
    Диапазоны `REP_RANGES` по цели/типу; адаптация 1–4 теперь с ростом повторов (чинит
    «недели идентичны»). Проверено harness'ом — таблица §2.4 сходится (W6 deload, W13 +подход).
  • **Рычаг «повторный ввод рабочего веса»** (ключевое): baseWeight хранится якорем `{w,c}`
    (цикл ввода). `currentWeight = w + (c_текущий − c_якоря)·шаг` → повторный ввод меняет ТОЛЬКО
    базу, всё пересчитывается от текущей недели, неделя/цикл/подходы/повторы НЕ сбрасываются;
    тост «Программа пересчитана… прогресс не сброшен». Старые числовые веса мигрируют (c=0).
  • **Инкременты весов**: карта `WEIGHT_STEP` по классу (низ +5, верх +2.5, гантели +2,
    изоляция +1–1.5) + половинный шаг если >5% веса + микро-шаг жен на верх/изоляцию.
  • **Дом (без веса)**: прогрессия по повторам/подходам + ручной переход на сложнее вариант
    (`harderVariant`/`applyVariant`, `v2_workout_variants`, кнопка в попапе) — чинит «дома
    нулевая прогрессия».
  • **Эргономика**: статус-баннер цикла (`statusBanner`/`programStatus`/`repBar`) — неделя,
    цикл, фаза, прогресс-бар повторов, «до +веса / до разгрузки»; плашки адаптации и разгрузки
    на токенах С2. Сохранены toggle «Сделано» и тост авто-изменений прошлого агента.
  • Сохранена совместимость: `_v2RenderWorkouts`/`_wkOpenExercise`/`_wkMarkDone` не тронуты
    по сигнатурам. `node --check` — OK. DOM-рендер не гонял (тяжёлый). НЕ деплоил.
  • ⚠ Открытые развилки для Замира — см. КАРТУ РЫЧАГОВ в отчёте чата (смена цели пересчитывает
    цикл; продвижение недели по завершению всех дней; авто- vs ручной переход дома).
  • **#2 вегетарианец (важный баг):** корень — `recipeHasExcluded` матчил только категорию
    ингредиента по КБЖУ-справочнику, а рыбу/мясо, помеченные в `tags.cats`/`tags.groups` или
    заданные ингредиентом не из справочника (сало), пропускал. Добавил `recipeCategorySet()`
    (ингредиенты + tags.cats + tags.groups, карта ярлыков `_GROUP_LABEL_TO_CAT`) и переписал
    `recipeHasExcluded` в cabinet.html и index.html. Питон-проверка по recipes.json: протечек
    по тегам было **40 → стало 0**; поймал и настоящий не-веган («…с салом»), fr_234784 (минтай)
    закрыт по двум путям. Побочно режутся мис-тегнутые веган-блюда — теги чистить в данных.
  • **#4 «Сделано» (workouts.js):** повторное нажатие теперь СНИМАЕТ отметку (toggle), отметка
    привязана к номеру недели → авто-сброс с новой неделей сохранён; добавил уведомление-тост
    об авто-изменениях прогрессии (снимок `v2_workout_snapshot` + `notifyAutoProgress`/`wkToast`).
  • **#1 витрина:** проверил — index уже дублирует день/план кабинета (тот же движок+разметка)
    и бар-меню (таб-бар `v2-tabbar`) на витрине НЕТ (гейт `IS_CABINET` в `v2-app.js`). Кода не
    менял; полный перенос нового таб-скина на витрину — отдельная дизайн-задача (см. бэклог #1).
- **11.07 (вечер-2)** — Перегенерил **14 якорей с жиром <21% калорий** под новый пол
  «жир набора = 21%». Индексы: #43,84,92,93,104,109,120,128,132,139,145,159,211,230.
  Спеки обновлены: Ж_new=round(0.21·kcal/9), У_new=round((kcal−4Б−9Ж_new)/4), Б и kcal
  без изменений. Дни пересобраны `regen_fat21.py` (та же дека что build_super_presets:
  без balance_off/снек-концентратов, Б≥1.2·Ж, есть в micronutrients_per_portion; ±3% по 4
  макро; UL 0.8; максимизация микро+сложные У+разнообразие). Ключевое: добавил ЛОКАЛЬНЫЙ
  ПОИСК (координатный спуск по макро) поверх random — он находит достижимую область, которую
  чистый random пропускал. **ИТОГ: ВСЕ 14 достигли строгих ±3%** (макро-откл макс ≤2.99%,
  микро 16.1–18.3, уник блюд 57–112), best-effort НЕ понадобился НИ ОДНОМУ, включая #84
  (прежний вердикт «недостижим» опровергнут — был артефакт random-метода). Влил дни обратно
  в `../anchors_seeds.json` (остальные 224 якоря не тронуты). `validate_presets.py` поправлен
  (28→30) и прогнан: **238×30, 0 битых id, 0 дней вне ±3%, 0 UL, микро 18.68** — ✅.
  Структурная проверка питоном: длина дня = числу слотов везде, 0 несуществующих id.
  Бэкап: `backups/snapshot_20260711_175439_перед-перегенерацией-якорей21/`.
- **11.07** — Прочитал СТАТУС + CLAUDE.md. Удалил устаревший `ОПТИМУМ.md`. Перенаправил
  `CLAUDE.md` на этот файл как точку входа + ввёл протокол «читай первым / пиши назад».
  Провалидировал супер-пресеты: цикл 30×3=90 чистый, 0 битых id; выявил дефект якоря #84
  (недобор до 40%, все 30 дней) — занёс как известную проблему. Обновил СТАТУС под 10.07.
  Перегенерил #84 (`fix_anchor84.py`, best-effort): макс-промах 37%→23%, микро 16.8, UL ок,
  бэкап сделан. Установлено: ±3% для #84 недостижим (хвост pct 0.04%) — решение (принять/
  смягчить цель) за Замиром. Структурная валидация всего файла: 0 битых, 238×30 цел.
- **11.07 (вечер)** — Разбор пола жира для набора. Референс: `РЕФЕРЕНС_макросы_якоря` (в outputs) —
  жир<20% у 10 якорей, углы у #84 в норме атлета, проблема в жире. Предложено жир набора
  1.0→1.2 г/кг (оздоровляет 10 якорей в ≥20%). Снят цифровой отпечаток мотора
  (`fingerprint_kbju.py`, `МОТОР_отпечаток_жир_2026-07-11.md`): при 1.0 vs 1.2 меняется цель
  у 2656 профилей, СМЕНА ЯКОРЯ у 2356 (19.5%!), не-наборные не тронуты (0). ВЫВОД: правку
  жира нельзя вносить без перегенерации целей якорей + пересборки пресетов. Ждём решения команды.
- **09.07 (ночь)** — предыдущая версия СТАТУСа: план 90 дней, тема С2, тренировки, gzip,
  CORS, фикс активности. (детали — в разделах выше).
