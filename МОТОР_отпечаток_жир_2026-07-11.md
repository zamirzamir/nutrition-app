# Карта-отпечаток мотора: эффект правки жира набора 1.0 → 1.2 г/кг

Дата: 11.07.2026 · Мотор НЕ тронут — это безопасный предпросмотр через реплику
`calculate()`+`pickAnchor()` (скрипт `fingerprint_kbju.py`). Правка НЕ применена.

## 1. Где цифра жира участвует в моторе (трейс)

`fatCoef` (cabinet.html ~стр.1986, `if (isGain) fatCoef = 1.0`) задаёт `state.fat`.
Дальше `state.fat` уходит в ТРИ узла мотора, не только в показ цели:

1. **Выбор якоря** — `pickAnchor()` (стр.3645): `uf = state.fat*9/kcal`, якорь берётся по
   минимуму `|Δбелок%|+|Δжир%|+|Δугл%|` внутри своей группы и числа приёмов. → сдвиг жира
   меняет доли → может выбраться ДРУГОЙ якорь (другой 30-дневный пресет).
2. **Потолок жира на порцию** — `maxWeightWithCaps()` (стр.3900/4036):
   `fatCap = min(50, round(state.fat*0.40))`. → выше дневной жир → выше лимит жира в блюде
   (60→24 г, 72→29 г на порцию) → допускаются более жирные блюда/порции.
3. **Ребаланс дня** — `rebalanceDayMacros()`/`finalMacroFit()` тянут день к `state.fat`.

Вывод: правка «одна строка», но эффект в трёх местах → нужен отпечаток до/после.

## 2. Отпечаток: сетка 12 096 профилей (пол×вес×рост×возраст×активность×цель)

Снят при жире набора **1.0** (base) и **1.2** (cand), затем diff.

| Метрика | Значение |
|---|---|
| Профилей сравнено | 12 096 (из них набор — 4 032) |
| Изменилась ЦЕЛЬ КБЖУ | **2 656 (22%)** — все только набор (лёгкие cw≤50 не меняются: пол жира 60/55 съедает разницу) |
| Сменился ВЫБРАННЫЙ ЯКОРЬ | **2 356 (19.5%)** |
| Наборных: жир поднялся с <20% в норму ≥20% | **148 профилей** |
| Изменились НЕ-наборные профили | **0** ✅ (правка изолирована в наборе — похудение/поддержание не тронуты) |

Примеры смены якоря (профиль: якорь было→стало, жир% было→стало):
`m55h165a40act gain: #43→#203 (20.5%→22.6%)` · `f75h175a50act gain: #148→#74 (24.8%→29.8%)` ·
`m80h180a30lig gain: #214→#169 (26.7%→32.1%)`.

## 3. Что это значит (главный риск, который поймал отпечаток)

**~1 из 5 профилей после правки садится на ДРУГОЙ якорь.** Это не баг, но:
если поменять формулу и НЕ перегенерировать цели якорей + не пересобрать пресеты, эти
2 356 профилей подтянутся к БЛИЖАЙШЕМУ СТАРОМУ якорю (с прежним низким жиром) — рассинхрон
между новой целью человека и старым пресетом. Поэтому правильный порядок:

1. Изменить `fatCoef` набора 1.0→1.2 (cabinet.html + index.html), пол 60/55 оставить.
2. **Перегенерировать цели 238 якорей** новой формулой (чтобы якоря отражали новый жир).
3. **Пересобрать супер-пресеты** `build_super_presets.py` на новых якорях.
4. Снять отпечаток ПОСЛЕ на живом моторе (Chrome/Node) и сверить с этим предпросмотром.
5. Замир заливает.

## 4. Что ещё не покрыто отпечатком (следующий слой)

Этот отпечаток — слой **цель + выбор якоря** (где цифра жира приземляется напрямую).
Слой **сборки дня и свайпов блюд** (`buildDayMenu`/`rebalanceDayMacros` + подмена блюд в
приёмах) считается уже в JS-моторе на реальных рецептах — его отпечаток снимается прогоном
той же сетки профилей через живой мотор (Chrome или Node), с записью КБЖУ дня, по-приёмного
КБЖУ и результата 2-3 свайпов на приём. Делаем на шаге 4, до/после — тем же diff.

## Файлы
- `fingerprint_kbju.py` — генератор (параметр: коэффициент жира набора).
- `fingerprint_base.json` (1.0) · `fingerprint_cand.json` (1.2) — снимки сетки.
