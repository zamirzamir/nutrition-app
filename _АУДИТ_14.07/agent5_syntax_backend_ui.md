# Аудит 14.07 — Агент №5: синтаксис/сборка + бэкенд + тренировки/UI

Режим: READ-ONLY. Node v22.22.3. Проверено: `app-v2/cabinet.html` (8542 стр., инлайн-скрипт ~7397 стр. с line 1128),
`app-v2/index.html` (7656 стр., инлайн ~6527 стр. с line 1110), `v2-app.js` (550), `v2-ui.js` (88),
`workouts.js` (870), `yandex-backend/index.js` (576), `report-bot/index.js` (99).

---

## 1. Синтаксис всего JS — ЧИСТО ✅

`node --check` по всем 7 целям (инлайн-скрипты извлечены regex-парсером `<script>` без `src`;
в каждом HTML ровно один инлайн-блок):

| Файл | Результат |
|---|---|
| cabinet.html инлайн (строки 1128–8525) | OK |
| index.html инлайн (строки 1110–7637) | OK |
| v2-app.js / v2-ui.js / workouts.js | OK |
| yandex-backend/index.js | OK |
| report-bot/index.js | OK |

**Синтаксических ошибок нет ни в одном файле.**

Подключение скриптов согласовано: обе страницы грузят `workouts.js`, `v2-ui.js`, `v2-app.js`
(cabinet.html:8538–8540; index.html:7650–7654).

---

## 2. Мёртвые/битые ссылки в коде

### 2.1 onclick / on*-хендлеры — битых НЕТ ✅
Проверены все 97 хендлеров cabinet + 84 index (и статические атрибуты, и `onclick=\"...\"` внутри
JS-шаблонов/innerHTML): **каждая вызываемая функция определена** (в инлайне или во внешних JS).
Кросс-файловые глобалы тоже сходятся: единственный «отсутствующий» на витрине
`saveProgressDebounced` (v2-app.js:486) везде под гардой `typeof === 'function'` — безопасно.

### 2.2 id, к которым обращается JS, но которых нет в разметке — 4 находки (все minor, под null-гардами)

| # | Где | Что | Severity |
|---|---|---|---|
| 1 | cabinet.html:6864–6875 (`toggleExclBanner`), index.html:6081 | Обращается к id `excl-rest`, `excl-more`, `excl-footer`, `excl-toggle-icon` — эти id НИГДЕ не создаются, и сама `toggleExclBanner` ниоткуда не вызывается. Мёртвый код от старой версии баннера исключений. | minor |
| 2 | cabinet.html:1596–1698 (`sendFeedback`, сброс полей), index.html:1511 | id `feedbackText`, `feedbackStatus`, `feedbackSendBtn` не существуют: модалка `feedbackModal` (cabinet:784) переделана на ссылку в Telegram без формы. `sendFeedback` не вызывается ниоткуда → весь путь `/feedback` с фронта мёртв (бэкенд-роут жив). | minor |
| 3 | index.html:1319, 1396 | `getElementById('calcBtn')` — id `calcBtn` нет ни на витрине, ни в кабинете; надписи «🔒 Зафиксировать план»/«⚡ Пересчитать» на кнопке никогда не меняются. Под гардой `if (btn)`. | minor |
| 4 | v2-app.js:533–543 (`renderToday`) | Блок навешивания hold-хендлеров на `.v2-dchip` внутри экрана «Сегодня» — мёртвый: чипы дней рендерятся только на «Питании» (`renderFoodDays`), в разметке «Сегодня» их нет. | minor |

Ложные срабатывания сняты: `exclInput`/`exclHint` создаются динамически через `inputRow(...)`
(cabinet:6734, 6763), `v2-screen-*` создаются в v2-app.js:188.

**Предложение (не чинил):** вычистить п.1–3 как мёртвый код (продолжение задачи P6e), либо вернуть
кнопку отправки в feedback-модалку, раз бэкенд `/feedback` и админ-выгрузка живы.

---

## 3. Бэкенд `yandex-backend/index.js`

### Роутинг — каждый маршрут имеет хендлер ✅
Все 13 `path.endsWith(...)` (строки 114–131) → существующие функции; порядок корректен
(`/feedback-done`, `/feedback-list` проверяются ДО `/feedback`, иначе endsWith перехватил бы).
Fallback 404 + общий try/catch на 500 — есть. Хвостовые слэши срезаются (109).

### Находки (риски, НЕ чинил)

| # | Где | Что | Severity |
|---|---|---|---|
| Б1 | index.js:118, 121 | **`GET /debug-last` и `GET /check?debug=webhook` БЕЗ авторизации отдают `_lastWebhook` целиком** (rawBody + data): email, телефон и `order_id` последнего плательщика. А `order_id` = КОД ВХОДА. Любой, кто опрашивает эндпоинт, может перехватить свежую оплату и войти в чужой кабинет. Память функции живёт между вызовами тёплого инстанса — окно реальное. Предложение: закрыть оба за `ADMIN_KEY` или удалить (отладка имени поля закончена 28.06). | **critical** |
| Б2 | index.js:260 | Проверка подписи webhook **fail-open**: `if (PRODAMUS_SECRET && !calibrate)` — если env `PRODAMUS_SECRET` пуст/потерян при передеплое, ЛЮБОЙ POST создаёт подписку на 95 дней (и атакующий сам знает order → сразу логин). Плюс рубильник `PRODAMUS_CALIBRATE=1` полностью выключает подпись. Предложение: fail-closed (нет секрета → 500), calibrate только вместе с ADMIN_KEY. | major |
| Б3 | index.js:144–181 | `dbUpsertSubscription`: read-then-write без транзакции — два одновременных webhook (ретраи Продамуса) могут задвоить `periods` или потерять продление. UPSERT также перезаписывает `prodamus_order_id` → повторная оплата меняет код входа (известная задача #42). | minor |
| Б4 | index.js:455–500 | `/admin-extend-subs`: логика корректна (никого не укорачивает, идемпотентна, DRY-RUN по умолчанию, fail-closed по ADMIN_KEY ✅). Риск: `target = paid_at + 95×periods`, а `paid_at` — дата ПОСЛЕДНЕЙ оплаты; двухпериодникам, платившим с разрывом, может насчитать больше, чем «честные 90×2 от первой оплаты». Полный SELECT без пагинации — ок для текущей базы, следить при росте. | minor |
| Б5 | index.js:203–238 | `/recipes-url` (presign): доступ строго `authEmail` + активная подписка ✅; SigV4 собран корректно (scope, encodeURIComponent, UNSIGNED-PAYLOAD, host-only headers). Риски по природе: ссылка живёт 1 час и передаваема; `s3_not_configured` при пустых env — мягкая деградация в 500. | ок/minor |
| Б6 | index.js:249–252 | Парсер form-urlencoded: `pair.split('=')` берёт только первый `=` — значение с `=` внутри (base64-подпись в теле) обрежется. Сейчас подпись берётся и из заголовка, но при смене формата Продамуса — тихий отказ. | minor |
| Б7 | index.js:79, 294 | Сравнение HMAC через `!==` (не timing-safe) в куке и подписи webhook. Практический риск низкий. | minor |
| Б8 | index.js:505–515 | `/feedback` без авторизации и без rate-limit → возможность заспамить YDB (5КБ×∞). Предложение: лимит по IP/куке или капча-токен. | minor |
| Б9 | index.js:435–449, 528 | Админ-хендлеры fail-closed (`!ADMIN_KEY → forbidden`) ✅; ключи админа нигде во фронте не светятся (grep по app-v2 — чисто) ✅. | ок |

Путей, «ломающих прод», в роутинге не найдено: неизвестный путь → 404, ошибки → 500 с логом.

### report-bot/index.js — чисто ✅
SQL-агрегаты и таймзона Алматы посчитаны корректно; ошибки со stage-диагностикой. Minor: при
незаданных `BOT_TOKEN`/`CHAT_ID` упадёт в catch с невнятным `ENOTFOUND` (можно валидировать env явно).

---

## 4. Тренировки `workouts.js`

Движок прогрессии (адаптация 1–4 → double progression с deload, монотонная неделя, baseWeight-якорь)
математически согласован: `automat()` (261–281) и `programStatus()` (318–332) считают цикл одинаково;
`currentWeight` (293–302) корректно отсчитывает циклы от якоря `{w,c}`, включая миграцию старого
формата-числа (252–258); ввод веса в адаптацию даёт `c=0` без скачка (496).

### Находки

| # | Где | Что | Severity |
|---|---|---|---|
| Т1 | workouts.js:371–397 (`buildWeek`), 520–549 (`markDone`), 751–753 (render) | **Дубли dayId ломают «Сделано» и двигатель недели.** `dayId = place + '_' + day.key` без индекса дня, а шаблоны в неделе ПОВТОРЯЮТСЯ: `experienced.loss = ['upper','lower','upper','lower']` (108–115) и любое `trainWeekly > числа шаблонов` реплицирует ключи (376). Итог: (а) отметка «Сделано» на дне 1 визуально зачёркивает и день 3 с тем же ключом; (б) `doneN = ids.filter(id => d[id]===wk).length` считает дубли выполненными → неделя из 4 «верх/низ» закрывается за 2 тренировки, неделя новичка на 7 дней — за 3. Прогрессия едет быстрее, чем человек реально тренируется. Предложение: `dayId = place + '_' + di + '_' + key` + миграция старых отметок (или считать по уникальным ключам). | **major** |
| Т2 | workouts.js:523–528, 539 | Toggle «Сделано» работает только ВНУТРИ текущей недели. Как только последняя отметка закрыла неделю (`setWeekCounter(wk+1)`), откатить случайный тап нельзя — счётчик недель не декрементится (монотонность — осознанный дизайн, но кнопка «нажми ещё раз, если случайно» в этом случае обманывает: повторный тап создаст отметку уже НОВОЙ недели). Предложение: разрешить откат недели, если снимается отметка, закрывшая её, в течение той же сессии. | minor |
| Т3 | workouts.js:647, 741 | `FREQ_RU` знает только старые ключи `gym_*`, а кнопки кабинета теперь `freq_0..freq_7` (cabinet.html:860) → в шапке «Под твой профиль: набор массы · · неделя N» — частота пустая, двойной разделитель. `trainWeekly` при этом парсится верно (196–199). | minor |
| Т4 | workouts.js:461–473 (правка #43, `wk-vid`) | В целом аккуратно: fallback на фото при `error`, `muted/playsinline/autoplay`, poster. Краевой случай: если `error` придёт ПОСЛЕ `loadeddata` (обрыв сети на луп-видео), `vid.remove()` оставит `img` с `display:none` → пустой квадрат вместо фото. Предложение: в error-хендлере возвращать `img.style.display=''`. | minor |
| Т5 | workouts.js:492–497 | Сохранение веса: `if (v > 0)` — нельзя стереть введённый вес (вернуться к «дом/без веса»); ввод `0`/пустого молча игнорируется без подсказки. | minor |
| Т6 | workouts.js:181 | `getProfile` ищет `.act-btn[data-grp="gym"].on` по всему документу (без `#activityGrid`) — сейчас коллизий нет, но селектор шире, чем в cabinet.html:2204. | minor |
| Т7 | облако | Все ключи тренировок синхронизируются в облако: `WK_KEYS` (cabinet.html:1810–1811) покрывает done/weights/variants/week/snapshot/suggested/congrats/prefs; гейт `_wkCloudReady` от затирания пустым локальным — на месте (1829–1832). ✅ | ок |

XSS в модуле: пользовательские строки идут через `esc()` (401), id упражнений — внутренние константы. Чисто.

---

## 5. Витрина `index.html` (демо-мотор, lazy-load, рассинхрон с кабинетом)

### Lazy-load / P0 — корректно ✅
- Витрина грузит публичный `demo-recipes.json` (~70 блюд) вместо приватного полного (index.html:3541–3550);
  микронутриенты — лениво с force-cache; провалы загрузок не роняют страницу (try/catch + warn).
- `fetchRecipesGated` (index.html:1625–1636): гейт `/recipes-url` → presign; фолбэк `'/recipes.json'`
  теперь приватен → для неоплативших упадёт 403 и словится catch в `ensureRecipesLoaded` (1779–1787);
  демо-массив уже загружен, ранний return защищает. Работает, но фолбэк-URL фактически мёртвый — minor.
- Тихий апдейт `version.json` (no-store) есть на обеих страницах (cabinet:1706, index:1551).

### Рассинхрон общих функций — ЕСТЬ, главная находка раздела

| # | Что | Severity |
|---|---|---|
| В1 | **Сегодняшний фикс #45 НЕ перенесён на витрину.** `portionCorridor` в index.html:3772 — СТАРАЯ версия: нет гейта `_isBaked` (`блины_оладьи`/`выпечка_десерт`), строка `блины_оладьи` в index.html вообще отсутствует. Демо-меню витрины для набора массы по-прежнему может показать оладьи ~420 г, которые в оплаченном кабинете уже режутся до 350 г — клиент увидит одно, купит другое. | **major** (для честности демо) |
| В2 | Системный дрейф движка: из 244 общих функций **53 различаются** телами. Ключевые содержательные: `portionCorridor` (В1); вся линейка `_ovKey` — в кабинете overrides per-slot (`mealOverrides[_ovKey(mealType, idx)]`, 9 использований), на витрине `_ovKey` нет вовсе (старый формат по mealType) — внутри себя витрина консистентна, но любой следующий перенос фиксов копипастой рискует смешать форматы. Остальные отличия — комментарии/витрин-специфика (login/оплата/демо). Это прямой аргумент за вынос движка в общий `engine.js` (задача P6b, pending #36). | major (системно) |
| В3 | `APP_VERSION` штампы различаются (cabinet `2026-07-06_143103` / index `2026-07-06_023106`) — нормально для раздельного деплоя, но подтверждает, что страницы деплоятся не атомарно (P6-риск). | info |
| В4 | v2-app.js:242–248: интервал 4с дёргает `renderToday()` (полный innerHTML) даже когда открыт другой таб; вместе с мёртвым блоком чипов (п.2.2-4) — лишний DOM-чёрн каждые 4с. На фоне известных #48/#49 (навигация дней) стоит объединить в один фикс. | minor |
| В5 | v2-app.js:361–376 `openDayInFood`: зачёркнутый день по тапу из ленты не открывается вовсе (условие `!dayStates[d].used`) — посмотреть меню пройденного дня нельзя. Связано с pending #48. | minor |

---

## Сводка по severity

- **critical: 1** — Б1 (неавторизованный `/debug-last` сливает email+код входа последнего плательщика).
- **major: 4** — Б2 (fail-open подписи webhook), Т1 (дубли dayId → неделя закрывается досрочно),
  В1 (фикс #45 не на витрине), В2 (дрейф 53 общих функций, нужен engine.js).
- **minor: ~14** — перечислены по разделам (мёртвый код, FREQ_RU, video-fallback, race в подписке,
  парсер webhook, /feedback без лимита и др.).
- **Синтаксис: 0 ошибок.** Битых onclick: 0. Битых id: только в мёртвом коде.
