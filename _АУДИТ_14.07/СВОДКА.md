# СВОДКА аудита приложения — 14.07 (5 субагентов Fable)

Полные отчёты: `agent1_presets_baked.md`, `agent2_micros_UL.md`, `agent3_recipes_validation.md`,
`agent4_engine_logic.md`, `agent5_syntax_backend_ui.md`.

## 0. Прямой вопрос Замира: пересобирать ли пресеты после снятия стретча?
**НЕТ, пересборка не нужна.** Веса в пресетах не хранятся (считаются в рантайме). Правка реально
отняла ёмкость у 357 дней из 7140 (7%); строгих регрессий 17 (0.24%), для персонажа 63→73 — 5 дней
с недобором 1.9–3.7% по калориям, и те соберутся в рантайме через замену одной оладьи
(`rebalanceDayMacros` умеет менять блюдо слота). Микронутриенты в пресетах: **0 нарушений UL** — билдер
`build_super_presets.py` отсекает UL-дни по построению (проверка на сутки, не на блюдо). Персонаж в безопасности.
Опционально (низкий приоритет): перегенерить ~13 якорей с 17 микро-рисками.

## 1. CRITICAL — безопасность, чинить ДО запуска
- **C1. `/debug-last` и `/check?debug=webhook` без авторизации** (yandex-backend/index.js:118,121) отдают
  последний webhook Продамуса: email + телефон + `order_id`. А order_id = код входа → перехват свежей
  оплаты = вход в чужой кабинет. → Закрыть за ADMIN_KEY или удалить.
- **C2. Проверка подписи webhook fail-open** (index.js:260): при потерянном env `PRODAMUS_SECRET` любой
  POST создаёт бесплатную подписку на 95 дней. → Сделать fail-closed (нет секрета → отказ).

## 2. MAJOR — логика/данные, влияет на клиента
- **M1. #48 (тап дня откатывается) — причина в `v2-app.js`, НЕ в движке.** Интервал каждые 4с зовёт
  `renderFoodDays()` → подсветка возвращается к `currentDay()` = первый незачёркнутый (v2-app.js:242-248,
  328-341, 465-472). Плюс вход во вкладку «Питание» открывает `currentDay()` (v2-app.js:285).
- **M2. Мой фикс #49 попал НЕ ТУДА.** Я поправил скрытую сетку `cabinet.html` (`renderDaysGrid`), а
  ВИДИМЫЕ чипы дней — в `v2-app.js:345-351`, у них защиты от свайпа нет. + 4-сек пересборка innerHTML
  отрывает чип посреди нажатия → `pointerup` теряется → таймер 750мс дожигает `crossDay(d)`. #48 и #49 —
  оба здесь. Чинить в v2-app.js: убрать/защитить 4-сек авто-пересборку, перенести guard на реальные чипы.
- **M3. Витрина (index.html) НЕ получила сегодняшний фикс #45** — демо всё ещё покажет оладьи 420г. Всего
  53 общие функции разошлись кабинет↔витрина (аргумент за вынос engine.js, задача #36).
- **M4. #46 подтверждён замером:** вег-колода (Б≥1.2Ж) — средний жир 21.6% ккал при цели набора ровно 21%;
  только 169 из 358 вег-блюд ≥21% жира → вег-дни системно висят на/за −3% по жиру. Плашка на первом
  рендере не зовётся → клиент не видит. Фикс: (а) плашка на первом рендере если >±3%; (б) глубже — добрать
  жирных вег-блюд в колоду / ослабить цель жира для вег.
- **M5. Тренировки: дубли `dayId`** (workouts.js:371-397,537-539) — шаблоны недели повторяются
  (upper/lower/upper/lower; новичок при freq≥4), id не включает индекс дня → «Сделано» на одном дне
  зачёркивает дубль, неделя из 4 тренировок закрывается за 2 → прогрессия едет быстрее реальных тренировок.
- **M6. `dataset.day` у кнопок дней не существует** → `renderDayMeals(NaN)` → откат на «День 1» после смены
  числа приёмов (cabinet.html:6251); `_rerenderDayView` вечный no-op (cabinet.html:8463).
- **M7. Замки перекусов затирают друг друга** — ключ по mealType, а snack-слотов 2-3 (cabinet.html:6466);
  «Съел» самоудаляется после F5; `frozen` не восстанавливается после пересборки.
- **M8. Последний фолбэк `pickMealForSlot`** (cabinet.html:4818) игнорирует `recipeHasExcluded` → мясо
  вегетарианцу в крайнем случае. Слова-исключения не проверяются по названию рецепта.

## 3. DATA — рецепты (чинить пакетом, не срочно)
- **D1. 126 мясных блюд типованы `выпечка_десерт`** (шаурма, лаваш-роллы, пироги с ветчиной, тушёная
  куриная печень), 31 в рабочей колоде. Пересекается с моей правкой (стретч снят для выпечка_десерт →
  эти мясные тоже капнулись 350г — не критично, но тег неверный, и печень тянет ретинол).
- **D2. Сало в словаре = «специи»/«масла_жиры»** → латентная дыра: новый рецепт с салом утечёт вегетарианцу
  (сейчас 4 спасают ручные теги).
- **D3. 40 рецептов с фантомными мясными тегами** (маскарпоне→рыба, сельдерей→рыба, куриное яйцо→мясо,
  овощной бульон→мясо) → 36 полностью вег-блюд скрыты от вегетарианца (минус к колоде, вкл. пачку омлетов/панкейков).
- **D4. 47 имён нет в `micronutrients.json`** → 117 рецептов (веганские vg_/poy_) молча теряют витамины в подсчётах.
- **D5. 14 рецептов энергобаланс >15%** (макс 25%, fr_1512 Форель); 11 супов без воды (оба борща); 3 рыбные
  закуски типованы «напиток»; 1 блюдо зря `balance_off` (fr_219888 деку проходит).
- **D6. Дубль в пресете:** якорь #189 — fr_19968 «Оладьи» дважды в одном дне (пересекается с #47).

## 4. RUNTIME MICRO
- **R1.** Рантайм не проверяет суточный UL при ручных заменах (только 4 макроса). Worst-case ручного
  стакинга: selenium ×2.57, ретинол-A ×2.25 (печень — тератоген), calcium ×2.40, iron ×2.08. Билдер
  пресетов это ловит, ручной своп через карусель — нет. Предложение: мягкая плашка при суточной сумме
  >0.9·UL по «токсичной пятёрке» (selenium, ретинол-A, iron, zinc, calcium), без жёсткой блокировки.

## 5. MINOR (в отчётах агентов, ~20 шт)
Мёртвый код (toggleExclBanner, форма фидбэка, calcBtn), гонка в dbUpsertSubscription, /feedback без
rate-limit, FREQ_RU не знает новых ключей freq_* → пустая частота в шапке тренировок, пустой квадрат при
обрыве wk-vid, пере-продление многопериодников от последнего paid_at, и др.

## Синтаксис
Чисто во всех 7 целях (`node --check`): cabinet.html, index.html, v2-app.js, v2-ui.js, workouts.js,
yandex-backend/index.js, report-bot/index.js. Битых onclick нет (проверен 181 хендлер).
