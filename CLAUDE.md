# Правила для этого проекта

## ПЕРВЫМ ДЕЛОМ: прочитать app-v2/СТАТУС.md — и писать в него назад

`app-v2/СТАТУС.md` — единственная точка входа и живая память проекта: над чем
работаем, что где залито, что изменилось в последних сессиях, кто в каком проекте
(боевой / v2) работает, известные проблемы и недоделки.

Протокол СТАТУСА (соблюдать КАЖДУЮ сессию, строго):
1. **Читай первым.** До любой работы прочитай `app-v2/СТАТУС.md` целиком.
2. **Пиши назад.** По ходу и в конце сессии дописывай в СТАТУС: что прочитал,
   что изменил, что залил, что осталось. Веди «Журнал сессий» внизу файла
   (дата + краткая запись). Так каждый новый чат продолжает без раскопок и все
   чаты работают из одной картины.
3. **Не начинай, не сверившись**, в каком проекте работать (боевой
   `roman-nutrition-app` / тестовый `roman-app-v2`) — чтобы не залить не туда.

Ориентир — всегда самые последние изменения (по датам файлов и Журналу сессий).
Старый `ОПТИМУМ.md` удалён 11.07 как устаревший снимок 22–23.06 — не воссоздавать.

## Критический консилиум (анти-поддакивание)

Когда Замир спрашивает мнение, оценку идеи или «прав ли я» — НЕ поддакивать.
Перед ответом прогнать позицию через четыре голоса: адвокат дьявола (аргумент
против), проверка фактов (что знаю точно vs предполагаю — при сомнении искать,
не угадывать), поиск слабых мест (скрытые допущения, риски), стресс-тест вывода
(когда он разваливается, есть ли контрпример).

В ответе: сначала прямая позиция (согласен / нет / зависит) и почему; затем плюсы
и минусы наравне. Хвалить только если заслужено. Несогласие — по делу и с
уважением. Не менять позицию из-за давления, только из-за нового аргумента или
факта. Честно признавать неопределённость.

Не применять к чисто техническим/фактическим задачам (расчёт, перевод, код без
спорной позиции).

Полная версия и обоснование: см. `КРИТИЧЕСКИЙ-КОНСИЛИУМ.md`.

## Добавление НОВЫХ блюд (соблюдать строго, каждый раз)

Новые блюда добавляются ТОЛЬКО через полный пайплайн `add_recipe.py` — по всем
рамкам и правилам. Нельзя просто «дописать блюдо» руками.

Обязательные правила:
1. **Источник.** Рецепт — только реальный, проверенный (кем-то приготовленный из
   интернета). Сами НЕ выдумываем. Название и шаги переписываем оригинально.
2. **Никакой варёнки в составе.** Все ингредиенты — сырые; варка/выпечка идёт в
   ШАГАХ (крупа сухая + вода и т.п.), иначе врут и вес блюда, и КБЖУ.
3. **6 весовых категорий.** Блюдо ОБЯЗАНО попасть во все 6 групп порций
   (`m70, m90, m90p, f60, f80, f80p`) — это делает `add_recipe.py`: считает порцию
   и КБЖУ на каждую группу из КБЖУ-на-100г × вес группы (от `base_portion_g`),
   плюс 26 микронутриентов на группу. **Полный (многопорционный) рецепт НЕ хранится**
   — только порция на 1 человека (`base_servings = 1`), развёрнутая в 6 групп.
4. **Валидация (внутри add_recipe.py).** Ккал ≈ 4Б+9Ж+4У (±15%); Б ≥ 1.2×Ж (иначе
   блюдо отрежет фильтр деки); UL-безопасность по всем группам; все ингредиенты
   известны базе (`ingredients_kbju.json` + `micronutrients.json`) с ТОЧНЫМИ именами;
   `meal_type` из {breakfast, lunch, dinner, snack}.
5. **Фото.** Генерим через Higgsfield (модель `nano_banana`, 21:9), конвертим
   PNG→webp (Pillow, quality 82), имя строго `<id>.webp` в `dish_photos/`,
   сверяем фото с рецептом.
6. **Заливка.** `./deploy.sh recipes.json` + фото отдельно
   (`aws … s3 cp dish_photos/<id>.webp …`). Микро-per-portion — офлайн, не заливается.

Как запускать: заполнить входной JSON (см. шаблон в `add_recipe.py`), затем
`python3 add_recipe.py файл.json --dry` (проверка) → без `--dry` (запись + бэкап).

## НЕ баг: порция «ниже минимума» — это намеренные потолки (проверено 06.07)

Иногда порция кажется ниже минимума коридора приёма (напр. запеканка 47 г, чизкейк
100 г при breakfast-min 180 г). Это НЕ баг — проверено вживую на 180 приёмах (лёгкая
женщина + мужчина): реальных нарушений 0. Все случаи «ниже cor.min» — это ЗАДУМАННЫЕ
потолки: десерты режутся до 100–140 г, орехи 20–30 г, плюс потолок белка/жира
(`maxWeightWithCaps` приоритетнее cor.min — см. комментарий в коде). Плюс у лёгкого
человека cor.min умножается на весовой коэффициент (перекус ≈ 43 г), так что 47 г —
внутри коридора. День всё равно добивается до ±3% (`rebalanceDayMacros`).

⚠️ НЕ «чинить» это, привязывая низ к cor.min — сломаются защитные потолки (получишь
180 г чизкейка, 200 г орехов в приём). Если реально увидишь вес ниже cor.min У БЛЮДА,
которое НЕ десерт/орех/сверхбелковое — вот тогда это баг, ищи причину.
