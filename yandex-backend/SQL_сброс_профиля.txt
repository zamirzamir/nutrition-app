-- СБРОС ПРОФИЛЯ КЛИЕНТА (для поддержки)
-- Если клиент по ошибке зафиксировал данные и просит сбросить — выполнить в консоли YDB.
-- ВАЖНО: телефон в формате 7XXXXXXXXXX (11 цифр, начинается с 7, без + и пробелов).

-- ============================================================
-- ВАРИАНТ А: полный сброс — клиент введёт все данные заново как в первый раз.
-- Удаляем профиль И прогресс (меню/дни). Подписка (оплата) НЕ трогается — доступ сохраняется.
-- ============================================================
DELETE FROM user_profiles WHERE phone = '7XXXXXXXXXX';
DELETE FROM user_progress WHERE phone = '7XXXXXXXXXX';

-- ============================================================
-- ВАРИАНТ Б: только разблокировать данные (оставить введённое, но снять "замок",
-- чтобы клиент мог отредактировать вес/рост/возраст/цель). Меню и прогресс сохраняются.
-- ============================================================
-- UPDATE user_profiles SET locked = '0' WHERE phone = '7XXXXXXXXXX';

-- ============================================================
-- ПРОВЕРКА: посмотреть текущее состояние профиля клиента.
-- ============================================================
-- SELECT phone, locked, data FROM user_profiles WHERE phone = '7XXXXXXXXXX';

-- ПОДСКАЗКА: подписку (оплату) НИКОГДА не удаляем при сбросе профиля,
-- иначе клиент потеряет доступ. Сброс профиля = только данные расчёта, не оплата.
