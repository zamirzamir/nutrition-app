# Тренировки v2 — спека прогрессии (реализуемая)

> Дата: **11.07.2026**. Для основного чата (реализация). Обоснование и ссылки —
> в `ТРЕНИРОВКИ_аудит_2026-07-11.md`. **Единицы: недели.** Всё — чистые функции
> от номера недели + сохранённого «рабочего веса», поэтому продление бесшовно.

---

## 1. Схема заказчика — точная формализация (как задано)

Заказчик: тот же вес, **+1 повтор/нед в течение 4 недель** → сброс повторов к исходным,
**+1 подход**; этот 4-недельный подъём повторяется **4 раза** (каждый раз +1 подход) →
сброс И повторов, И подходов, **+вес** → и так бесконечно.

### 1.1 Конечный автомат (as-is заказчика)
Пусть `W` — номер тренировочной недели ПОСЛЕ адаптации (W = 1,2,3,…; недели адаптации
1–4 — отдельно, см. §4). Обозначим:
- `R0` — стартовые повторы, `S0` — стартовые подходы, `ΔW` — инкремент веса, `Wbase` — рабочий вес.
- Макроцикл = **16 недель** (4 блока × 4 недели).

```
i          = W - 1                 // 0-based
macro      = floor(i / 16)         // сколько раз уже подняли вес
inMacro    = i mod 16              // 0..15 позиция внутри макроцикла
block      = floor(inMacro / 4)    // 0..3  — номер сет-блока
weekInBlk  = inMacro mod 4         // 0..3  — позиция подъёма повторов

reps   = R0 + weekInBlk            // R0 .. R0+3
sets   = S0 + block                // S0 .. S0+3
weight = Wbase + macro * ΔW
```

### 1.2 Таблица (as-is, пример S0=2, R0=8, ΔW=шаг)
| Нед | block | reps | sets | weight | Нед | block | reps | sets | weight |
|----|--|----|--|-------|----|--|----|--|-------|
| 1 | 0 | 8  | 2 | +0ΔW | 9  | 2 | 8  | 4 | +0ΔW |
| 2 | 0 | 9  | 2 | +0ΔW | 10 | 2 | 9  | 4 | +0ΔW |
| 3 | 0 | 10 | 2 | +0ΔW | 11 | 2 | 10 | 4 | +0ΔW |
| 4 | 0 | 11 | 2 | +0ΔW | 12 | 2 | 11 | 4 | +0ΔW |
| 5 | 1 | 8  | 3 | +0ΔW | 13 | 3 | 8  | 5 | +0ΔW |
| 6 | 1 | 9  | 3 | +0ΔW | 14 | 3 | 9  | 5 | +0ΔW |
| 7 | 1 | 10 | 3 | +0ΔW | 15 | 3 | 10 | 5 | +0ΔW |
| 8 | 1 | 11 | 3 | +0ΔW | 16 | 3 | 11 | 5 | +0ΔW |
| — | — | — | — | — | **17** | 0 | 8 | 2 | **+1ΔW** ← новый макроцикл |

Продлевается бесконечно: неделя `17+k` = та же таблица со сдвигом `macro`.

### 1.3 Вердикт по схеме заказчика (проверка ресёрчем)
**Направление верное, тайминг и потолок — нет. Рекомендую скорректировать. Что не так:**

1. **16 недель без прибавки веса — слишком долго для новичка/среднего.** Классическая
   double progression и правило NSCA 2-for-2 двигают вес в пределах ~2–4 недель, а не 16.
   У новичка «newbie gains» — вес можно и нужно поднимать раз в 1–4 недели. 16 недель на
   одном весе недогружают и демотивируют.
   *Смягчающий факт:* пока лезут повторы и подходы, тоннаж (вес×повторы×подходы) всё же
   растёт каждую неделю — то есть прогресс есть. Но абсолютный вес трогать раз в 16 нед —
   всё равно чрезмерно долго на весовой оси.
2. **Ползучесть подходов до 5 — риск объёма и перетрена.** S0+3 = 5 подходов на КАЖДОМ
   упражнении. Пример: набор, 2 упражнения на мышцу, 2×/нед → 5×2×2 = **20 подходов/мышца/нед**
   = MRV. Без разгрузки это гарантированный застой/перетрен (см. аудит §1.3, 1.4).
3. **Нет разгрузки.** 16 недель монотонного роста объёма без deload противоречит консенсусу
   (разгрузка каждые 4–8 нед).
4. **Порядок «повторы → подходы → вес» стекается неверно.** Повторы и подходы — разные
   рычаги. Стандартно: повторы гоняем внутри диапазона → добавили вес (2–4 нед цикл).
   Подходы — отдельный, медленный, капнутый рычаг мезоцикла, а не ×4 до потолка перед весом.

**Итог: сохранить ИДЕЮ (повторы → подходы → вес, детерминированно, бесконечно,
продлеваемо), но сжать макроцикл 16 → ~6 нед, капнуть подходы, добавить разгрузку.**
Ниже — рекомендуемый автомат.

---

## 2. РЕКОМЕНДУЕМЫЙ автомат (компактная double progression + капнутые подходы + deload)

### 2.1 Идея
- **Rep-цикл**: повторы лезут `R0 → Rmax` (по +1/нед). Как дошли до `Rmax` — **сброс к R0
  и +ΔW к весу** (это и есть двойная прогрессия, вес двигается раз в цикл ≈ 3–5 нед).
- **Подходы**: медленный капнутый рычаг — +1 подход раз в 2 цикла, потолок `S_cap = S_work+1`.
- **Разгрузка**: каждая 6-я тренировочная неделя — deload (легче, меньше объём).
- **Дом (без веса)**: роль «ΔW» играет **переход на более сложный вариант упражнения**
  (`lvl+1`) в конце цикла; если сложнее варианта нет — растим только повторы/подходы.

### 2.2 Константы (по цели и типу упражнения)
Диапазон повторов `[R0..Rmax]` (длина цикла `Lc = Rmax−R0+1`):

| Цель | compound | iso | Lc (compound) |
|---|---|---|---|
| Набор (gain) | 8→12 | 10→14 | 5 |
| Похудение (loss) | 12→15 | 12→16 | 4 |
| Поддержание (maintain) | 10→13 | 12→15 | 4 |

Подходы: `S_work` = compound 3 / iso 2–3 (как в текущем `GOAL_SCHEMES`), `S_cap = S_work+1`
(max 4 для compound). Core — по времени/повторам, подходы не капаем выше 3.

### 2.3 Инкремент веса `ΔW` (по науке, §1.8 аудита)
| Класс упражнения | Примеры id | ΔW (кг) | ΔW (%) |
|---|---|---|---|
| Штанга, низ тела | g_squat, g_rdl, g_hip_thrust | **+5** (или +2.5 если тяжело) | 2.5–5% |
| Штанга, верх тела | g_bench, g_ohp | **+2.5** | 1.5–2.5% |
| Гантели (жимы/тяги) | g_db_press, g_incline_db, g_db_row | **+2** (по +1 кг на гантель), можно +1 | 2–4% |
| Тренажёр низ | g_leg_press | **+5**; сгиб/разгиб g_leg_curl/g_leg_ext **+2.5** | шаг стека |
| Тренажёр/блок верх | g_lat_pulldown, g_row_cable, g_cable_fly | **+2.5** | шаг стека |
| Изоляция мелкая | g_curl, g_hammer, g_lateral, g_triceps_rope | **+1–2** (микроблины) | ≤5% |

**Половая поправка (единственная обоснованная, §1.9):** если `ΔW > 5%` текущего рабочего
веса (типично для женщин на верх/изоляции) — **брать половинный шаг / микроблин 0.5–1 кг**.
Это не «женская программа», а защита от слишком крупного относительного прыжка.
Правило универсальное (применимо и к лёгким мужчинам), но чаще срабатывает у женщин.

**Общий предохранитель:** если пользователь отметил, что вес «еле доделал» (RIR 0) — не
поднимать в этом цикле (повторить вес). Это программный аналог 2-for-2 «в обратную».

### 2.4 Псевдокод (рекомендуемый)
```
// W — тренировочная неделя после адаптации (1,2,3,...), монотонно растёт, БЕЗ капа.
// Разгрузка — каждая 6-я неделя.
function progression(ex, W, goal) {
  const isDeload = (W % 6 === 0);
  // «эффективная» неделя прогресса = недели без учёта разгрузок
  const e = W - Math.floor(W / 6);          // 1,2,3,4,5, (6=deload), 6,7,8,9,10,(12=deload)...
  const [R0, Rmax] = repRange(goal, ex.kind);
  const Lc = Rmax - R0 + 1;

  const c = Math.floor((e - 1) / Lc);       // индекс цикла, 0-based
  const r = (e - 1) % Lc;                    // позиция внутри цикла 0..Lc-1

  let reps   = R0 + r;
  let sets   = Math.min(S_cap(ex), S_work(ex) + Math.floor(c / 2)); // +1 подход раз в 2 цикла, до cap
  let weight = ex.hasWeight ? Wbase(ex) + c * deltaW(ex) : null;    // вес двигается раз в цикл
  let variant = ex.id;                       // для дома: см. §2.5

  if (isDeload) {                            // разгрузочная неделя
    sets   = Math.max(2, sets - 1);
    reps   = R0;                             // низ диапазона, легче
    // weight: держим прошлый вес (не растим); UI помечает «разгрузка»
    return { sets, reps, weight, variant, phase: 'deload' };
  }
  return { sets, reps, weight, variant, phase: (r === Lc-1 ? 'peak' : 'build') };
}
```

Пояснения:
- **Вес** = `Wbase + c*ΔW`. Цикл ≈ Lc недель (4–5) → вес двигается раз в ~месяц. Реалистично.
- **Подходы** капнуты: `S_work → S_work+1`, добавляется после 2-го цикла и держится. Никогда не 5.
- **Разгрузка** каждые 6 недель: −1 подход, повторы к низу, вес держим. Не «сброс», а «легче».

### 2.5 Дом (bodyweight) — прогрессия без железа
У домашних упражнений `hasWeight=false`. Вместо `ΔW`:
- Внутри цикла — растим повторы `R0→Rmax` (как в §2.4) и подходы.
- **В конце цикла (аналог +веса)** — если есть более сложный вариант той же группы с
  `lvl = текущий+1` (напр. `h_pushup_knee(lvl1) → h_pushup(lvl2) → h_diamond(lvl3)`;
  `h_squat → h_bulgarian`) — **переключить упражнение на сложнее и сбросить повторы к R0**.
  Хранить `variantId` в состоянии (см. §3). Если сложнее варианта нет — продолжаем растить
  повторы/подходы (потолок повторов можно поднять, напр. Rmax+5 для калистеники).
- UI (см. аудит §4.5) предлагает переход явной кнопкой, чтобы человек контролировал момент.

### 2.6 Таблица (рекомендуемая, пример: набор, compound, R0=8 Rmax=12 Lc=5, S_work=3)
| Нед W | эфф. e | цикл c | reps | sets | вес | фаза |
|--|--|--|----|--|-----|---|
| 1 | 1 | 0 | 8  | 3 | +0ΔW | build |
| 2 | 2 | 0 | 9  | 3 | +0ΔW | build |
| 3 | 3 | 0 | 10 | 3 | +0ΔW | build |
| 4 | 4 | 0 | 11 | 3 | +0ΔW | build |
| 5 | 5 | 0 | 12 | 3 | +0ΔW | peak |
| **6** | — | — | 8 | 2 | +0ΔW | **deload** |
| 7 | 6 | 1 | 8  | 3 | **+1ΔW** | build |
| 8 | 7 | 1 | 9  | 3 | +1ΔW | build |
| 9 | 8 | 1 | 10 | 3 | +1ΔW | build |
| 10| 9 | 1 | 11 | 3 | +1ΔW | build |
| 11| 10| 2 | 8  | **4** | **+2ΔW** | build ← +1 подход (c=2) |
| **12**| — | — | 8 | 3 | +2ΔW | **deload** |
| 13| 11| 2 | 9  | 4 | +2ΔW | build |
| … | … | … | …  | … | … | … |

За ~год (52 нед): ~8–9 циклов → **+8–9 шагов веса** (низ ≈ +40–45 кг, но НЕ за 2 месяца,
а за год, с разгрузками — реалистично для новичка→среднего), повторы циклируют 8→12,
подходы 3→4. Сравни с текущим кодом: +45 кг за 8 недель (нереально) → §2.4 аудита.

### 2.7 Адаптация (недели 1–4) — до старта автомата
Оставить `ADAPT_SCHEME` (2 подхода, RIR 3–4, без отказа). Опциональное улучшение
(чинит дефект «недели 1–4 идентичны», аудит §2.3): внутри адаптации тоже +1 повтор/нед,
напр. 2×12 → 2×13 → 2×14 → 2×15. Вес не гоним. Затем W=1 автомата = неделя 5 календаря.

---

## 3. СОСТОЯНИЕ ДЛЯ ХРАНЕНИЯ (бесшовное продление)

Ключевой принцип: **всё вычисляется как чистая функция от (номер недели, сохранённый
рабочий вес/вариант)**. Тогда продление плана = просто продолжает расти счётчик недель,
ничего не пересчитывается заново.

### 3.1 Глобальное состояние (на пользователя)
| Ключ | Что | Зачем |
|---|---|---|
| `wk_weekCounter` | монотонный счётчик тренировочных недель, **только растёт, БЕЗ капа** | вход автомата; продление не сбрасывает |
| `wk_weekAnchor` | как считается неделя: по **выполненным тренировкам** (реком.), а не по дням питания | чинит разрыв аудита §2.5 |
| `planDays` / `wk_paidWeeks` | оплаченный горизонт (для показа/гейта) | **только видимость**, НЕ влияет на прогрессию |

Замена текущей `currentWeek()` (сейчас = дни_питания/7, кап ~13): считать по числу
недель с ≥1 (или ≥N) отмеченной тренировкой; либо по недельным «Сделано». Кап убрать.

### 3.2 Состояние на упражнение (`exId`)
| Поле | Что | Пример |
|---|---|---|
| `baseWeight` | введённый пользователем рабочий вес (кг), точка отсчёта | 40 |
| `variantId` | текущий вариант движения (для дома/сложности); по умолчанию = id | `h_pushup` |
| `weightAnchor` *(опц.)* | `{week, weight}` — если пользователь вручную поправил вес | `{week:12, weight:47.5}` |
| `hardFlag` *(опц.)* | «в этом цикле вес еле доделал» → не поднимать в конце цикла | true |

`baseWeight` уже хранится в `localStorage.v2_workout_weights` — **менять не надо**, он
переживает продление. Достаточно добавить `variantId`/`weightAnchor` при желании
адаптивности.

### 3.3 Почему продление бесшовно
- Прогрессия = `f(wk_weekCounter, baseWeight, variantId)`. Продлили план (доплата) →
  `planDays` вырос → `wk_weekCounter` продолжает инкрементироваться дальше → автомат
  считает следующие циклы **с той же точки**, без рестарта.
- Ничто в прогрессии не завязано на `planDays`/`90`. Единственная связь — **видимость**
  дней/гейт оплаты. Поэтому «доплата добавляет дни к текущему плану, не начинает заново»
  выполняется автоматически, если убрать кап недели и не привязывать автомат к 90.
- **Критично при реализации:** нигде не делать `week = week % 90` или сброс счётчика при
  достижении `planDays`. Счётчик недели — сквозной на всю историю пользователя.

### 3.4 Миграция с текущего кода
1. Заменить `currentWeek()`: источник — выполненные тренировки, без капа.
2. Заменить `progressionSteps`/`currentWeight` на автомат §2.4 (вес раз в цикл, не/нед).
3. Добавить ветку `hasWeight=false` (дом) → прогрессия по повторам/подходам/варианту (§2.5).
4. Добавить deload-неделю (каждая 6-я) + UI-плашку.
5. `baseWeight` из `v2_workout_weights` переиспользовать как есть (обратная совместимость).

---

## 4. Мини-резюме реализации
- **Автомат:** повторы `R0→Rmax` по +1/нед → на пике сброс + **вес +ΔW раз в цикл** (4–5 нед),
  подходы `S_work→S_work+1` раз в 2 цикла (cap 4), **deload каждые 6 нед**.
- **Инкременты:** низ штанга +5, верх штанга +2.5, гантели/блоки +2/+2.5, изоляция +1–2;
  если шаг >5% веса — половинный (микроблин). Дом: вместо веса — переход на сложнее вариант.
- **Хранить:** сквозной `wk_weekCounter` (без капа) + `baseWeight`/`variantId` на упражнение.
  Продление = счётчик едет дальше, прогрессия не рестартует.
- **Схему заказчика 16-нед сжал до ~6-нед макро** (вес раз в цикл, а не раз в 16 нед),
  подходы капнул на 4 (а не 5), добавил разгрузку — обоснование в §1.3.
