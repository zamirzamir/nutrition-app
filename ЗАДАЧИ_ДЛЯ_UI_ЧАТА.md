# Задачи для другого чата (UI + 90 дней + цена)

Скопируй это целиком в новый чат. Всё ниже — ещё НЕ сделано.

## Контекст проекта (обязательно)
- Приложение: калькулятор КБЖУ + план питания. Два зеркальных single-file vanilla-JS файла: `cabinet.html` (кабинет клиента, `IS_CABINET=true`) и `index.html` (витрина, `IS_CABINET=false`). ЛЮБУЮ UI-правку делать в ОБОИХ файлах одинаково. Перед правкой — бэкап файла.
- Деплой владелец делает сам (`./deploy.sh`). Не деплоить.
- Данные: `recipes.json` (рецепты), `anchors_seeds.json` (пресеты — только ID блюд по дням), `micronutrients_per_portion.json`. КБЖУ считается на лету, движок ребалансит день до ±3%.
- 6 весовых групп: m70/m90/m90p/f60/f80/f80p. `weightGroup()` — по полу+весу. `portionCorridor()` — коридор веса; `tags.fixed_g` = вшитый фикс-вес; `maxWeightWithCaps()` — потолки Б/Ж и спец-лимиты.
- Тестовый профиль: мужской, 60 кг, цель 70, рост 170, 35 лет, лёгкая активность → группа m70.
- Есть механика «Готовлю на 2 дня» (искать `slot._cook2x2`, `fmtAmountMul`, `_weightMulHtml`): при включении список ингредиентов и закупка ×2, но КБЖУ и вес порции на день ×1.

## UI-фичи (оба файла, зеркалить)

1. КНОПКИ СМЕНЫ БЛЮДА — крупнее область нажатия, сам визуальный размер тот же. Увеличить кликабельную зону (padding + отрицательный margin, или `::before` hit-area).

2. КНОПКА «маленький вес?» под весом блюда (`.recipe-weight`, искать `renderDayMeals`/`_weightMulHtml`, ~строка 5400 cabinet). Условие: обед < 200 г ИЛИ ужин < 220 г. По клику — подсказка: «Съешь этот приём вместе со следующим перекусом. Перекус обычно весит не больше 200 г.»

3. БЛОК «Сейчас вы съели» — когда на блюде нажата кнопка «Съел». Вывести надпись «Сейчас вы съели вот столько КБЖУ 👇» и 4 кубика: Ккал <съедено> из <цель>; Белки; Жиры; Углеводы. Съедено = сумма КБЖУ блюд с нажатым «Съел». Цель = `state.kcal/protein/fat/carbs`.

4. ТУМБЛЕР «Готовлю на 3» — только на 1-м приёме: цикл 1→2→3→2→1→2→3… (каждый клик по кругу). 2-й приём — только «на 2» (1↔2). 3-й — тумблера нет. По аналогии с «Готовлю на 2» (`_cook2x2`): для ×3 список ингредиентов и закупка ×3, КБЖУ и вес порции ×1 (перечёркнутый оригинал + число ×3, как уже для ×2).

5. ОВОЩИ-ИНТЕРАКТИВ.
   (a) В шагах рецептов, где в ингредиентах овощ с весом 0 (id: «овощи», «огурец», «помидор», «салатные листья», «брокколи», «капуста» и т.п.) — дописать подсказку: «Овощи на КБЖУ не влияют, но дают витамины — можно добавить 1–2 овоща и съесть отдельно.» (можно и данными в recipes.json).
   (b) В списке покупок (неделя/28–90 дней): если такие блюда есть в меню — ПЕРВОЙ строкой кнопка «В меню есть блюда, куда овощи можно добавить по желанию. Добавить?». По «Да» — окно «Мы добавили: доп. огурцы N шт, доп. помидоры N шт, брокколи N, салат N, квашеная капуста 50 г» (по 1 овощу на каждое такое блюдо; квашеную капусту оформить как овощ, если её нет — добавить). Эти строки появляются ПЕРВЫМИ в списке.

## Приложение под 90 дней
- Сейчас план 28 дней («28 дн» зашито в ~15 местах обоих файлов). Сделать 90: навигация/счётчик дней 1–90; подписи «28 дней» → «90 дней».
- Пресеты — 30-дневный блок, повтор: день N → индекс в пресете = ((N-1) mod 30) + 1. (Пересборку пресетов делает владелец скриптом.)

## Цена 299 → 1499 ₽
- «299 ₽» → «1499 ₽» в 9 местах. cabinet.html ~строки 851, 862, 962, 1123, 1138, 1190, 7171, 7327; index.html ~851 (остальные — поиском «299»).
- Зачёркнутую «999 ₽» — поднять (напр. 2990 ₽) или оставить (решить).
- `PAYMENT_URL` (cabinet ~1344, index ~1406) = `'https://payform.ru/74bSKMr'` → новая ссылка Prodamus на 1499 ₽ (владелец создаст).
- Срок доступа сейчас 31 день (бэкенд — Yandex Cloud Function, НЕ в этих файлах). При 90-дневном плане решить: 31 день или дольше.

## Уже сделано (не трогать)
Все правки данных рецептов (шаги, веса, вода, бренды, дрожжи, бульоны, дубли, сахар, ванилин, округление 0.1) и снек-коридор (120–230) сделаны в основном чате. Детали — `ОТЧЕТ_НОЧЬ.md`, `ОСТАЛОСЬ_ТЗ.md`.
